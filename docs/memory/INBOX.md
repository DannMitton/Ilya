# INBOX — caught, not lost

**Append-only. One line per item. Claude does not open this file unless Dann asks
for it.**

This exists so Dann can surface a thought mid-task and let it go immediately,
knowing it is held. Nothing here is scoped, costed, or ruled on. **An item leaves
this file only when Dann rules it into the tracker in `STATE.md`.** That is the
only gate, and it is what stops the backlog being generated by the process itself.

Format: `I.nn | YYYY-MM-DD | one line, in Dann's words where possible`

---

| id | date | the thought |
|---|---|---|
| I.01 | 2026-08-14 | Mobile: Notation is anchored to the bottom and opens fully expanded, so you cannot reach the metadata fields without closing it. Its open state should be closed. |
| I.02 | 2026-09-01 | "I must have dismissed this dialog box ten times. Why does it persist?" The install prompt never saves a dismissal on desktop: `InstallPrompt.svelte:76` guards the only write behind `if (isIos)`, and the iOS path writes `sessionStorage`, which dies with the tab. Every singer on a stable URL is asked again on every load, forever. Found on the 510a280 walk. **NUMBERED N.105 by Dann 2026-09-01.** |
| I.03 | 2026-09-01 | "Why is there no meter given?" Ilya draws no time signature anywhere. `packages/score-parser/src/staff-renderer.ts:964` reads it for spacing only, and the SMuFL digits at `:356-359` are spent on N.104's tacet count. Without Sun 1 draws a duplet bracket that cannot be read without its 12/8. **RULED BY DANN 2026-09-01, on the 510a280 walk: "Meter belong[s] in the loupe head, yes."** So when a metre is drawn it is emitted with the clef and key, before the first music mark, and tonight's paint-order gate carries it into the head with no new code. That is now ruled rather than inferred. **HIS RULING DOES NOT REACH A MID-SYSTEM METRE CHANGE**, which is drawn after the first music mark and belongs with the music rather than in the head; the gate would put it in the head, which would be wrong. Still open, and still his. Found on the 510a280 walk. |

---
*Created 2026-08-13.*
2026-08-18: Mention WCAG accessibility in Ilya's marketing capabilities list (Dann, GUI session).
2026-08-20: Let every section header be retractable, metadata included (Dann, N.73 S3 walk).
2026-08-20: Soften the sage and lavender borders on the drawer input fields; keep the semantics, ask for a lighter shade of each (Dann, N.73 S3 walk).
2026-08-20: Make the placeholder text in the drawer input fields formatted consistently; they must match each other (Dann, N.73 S3 walk).
2026-08-20: A hamburger menu beside the sigil for singers who prefer that navigation: Drawer, Transcribe, Score Markup, Learn, Guide, Voice profile, Eng/Fr (Dann, N.65 walk). His own question: is this too much?
2026-08-20: The word "Français" sits inside an English document at HeaderBar.svelte:58 with no lang attribute on it, so a screen reader pronounces it with English phonetics. WCAG language-of-parts. Applies whichever language-toggle pattern is chosen (Dann, N.65 walk).
2026-08-21: Re-examine transitions: the drawer opening, and moving between screens. Dann wants options weighed against what serves the app and what is affordable in processing power (Dann, N.77 walk).
2026-08-23: [u] is hard to capture and keeps landing Provisional (screenshot, cardinal-u 301/759 Hz). Dann asks: mic sensitivity? is the low cutoff low enough for a bass fR1 on [u]? what else would make [u] capturable without provisional values? Read the N.49 document before opening the extractor (Dann, colon-audit walk).
2026-08-23: In the calibration wizard, the rule under Back and above the Voice 1 selector is sage; Dann rules it lavender, to match the rule beneath the selector (Dann, N.80 walk, screenshot on ilya-cmouwkt5d).
- 2026-08-24 Dann proposes: dead-code audit, then delete what it finds.
- 2026-08-24 Dann proposes: optimization pass; perceive, confer with Dann, then execute.
- 2026-08-24 Dann proposes: structural-intuitiveness pass for open-source contributors.
- 2026-08-24 Dann proposes: curated walkthrough, IMMINENT; a colleague asked this week.
- 2026-08-24 Dann proposes: marketing materials; Ilya exists, free, open source.
- 2026-08-24 Dann proposes: redevelop the Guide; prose and images describe an older build.
- 2026-08-24 Dann asks: colour print prints greyscale except the flag; intentional?
- 2026-08-24 Dann asks: headers/footers on emitted documents; Ilya provenance plus dannmitton.com.
- 2026-08-24 Dann asks: can a smartwatch capture formant readings and relay them to Ilya on a phone or laptop?
- N.90 2026-08-24: the photograph tier, mapped by the OMR field report and vocal-line brief (Fable, other session; to be filed in docs/sessions). NUMBERED by Dann 2026-08-24.
- N.91 2026-08-24: the piano-doubling witness as pitch verifier, and OpenScore Lieder as the CC0 accuracy-benchmark corpus. NUMBERED by Dann 2026-08-24.
- N.92 2026-08-24: notation editing tools; correct the read notation in place. Dann: no point in Score Markup without prescriptive vowel values on the correct pitches. NUMBERED by Dann 2026-08-24.
- N.93 2026-08-24: easy text entry interfaces for the poem and underlay. Same ruling, same night. NUMBERED by Dann 2026-08-24.
- N.94 2026-08-24: transposition interface, modelled on Newzik; prior rulings 9-14 and the detented-ruler spec are in claude/e31-late-rulings-and-the-transposition-control_2026-08-07.md, 17 days old, amendments unchecked. NUMBERED by Dann 2026-08-24.
- N.98 (numbered by Dann 2026-08-24, not yet placed): voice formant profile selector. A dropdown for teachers with busy studios to switch among multiple stored voice profiles, modelled on the library save function.
- N.99 (numbered by Dann 2026-08-24, not yet placed): skew-tolerant staff detection. On the Kabalevsky op. 52 no. 5 plate the primary row detector self-abstains (gate 0.0026 under the 0.015 floor, the reader.py:370-381 signature) and the slice fallback loses 6 of 55 line rows (system 4's whole voice staff, one line of system 3's piano staff), so the ruled loud abstention at reader.py:540 fires. Every line prints; the gap is skew tolerance, likely deskew before row projection or per-staff line completion.
- N.100 (numbered by Dann 2026-08-24, not yet placed): the PDF route must not trust the media box. Kabalevsky op. 52 no. 5 declares its 300 dpi A4 scan at 96 ppi, so the 400 dpi rasterization makes a 151-megapixel image and the route dies before the reader runs. Fix direction: rasterize toward the embedded image's native pixel count, or cap the raster and warn.
- Restore pacifier (Dann, 2026-08-25, mid N.97b walk): a reload takes a few seconds before the song reappears; offer a busy indicator saying nothing is wrong or lost.
- ~~Portrait hint placement (Dann, 2026-08-25, mid N.92 slice 2 walk): centre "Turn your device sideways" in the blank page instead of the top edge, for prominence.~~ STRUCK 2026-08-26, mobile slice 1: the blank page it would have centred a hint in now renders the score, and the hint is deleted. Struck rather than removed, because this file says an item leaves it only when Dann rules it into the tracker.
- Mobile pitch editing (Dann, 2026-08-25, recorded as track switch): locate-then-edit across two screens is untenable on a phone; option floated: Speedy Entry controls on an invoked keyboard. Belongs to N.92/N.93 design; Dann says this blocks release. Fable conversation deferred past the Thursday walkthrough.
- Mobile navigation (Dann, 2026-08-25, recorded as track switch): navigation on mobile is abysmal, needs help. Same conversation as the mobile pitch editing line above: how a phone drives Ilya at all. Own session after the Thursday walkthrough, Studio ruling open first.
- Print button under the paper on a phone (Code, 2026-08-26, slice 1 memo): `.paper-fit` flex-shrinks 1528 to 793.4 px so the Print button lands beneath the paper in portrait. One-liner `flex-shrink: 0` but it touches both Studio documents; Dann to rule when it rides.
- Floor-commit restore flakiness (Code, 2026-08-26, slice 1 memo): on `98bba71` the score reverted to the empty envelope in 3 of 6 automated runs; 5 of 5 held on the slice 1 tree. Unexplained by a CSS deletion; possibly the harness. Worth its own look.
- Tether 20 draft, HELD BY DANN 2026-09-01, not written into CONTRACT.md. "Before you change how a number is computed, find every other site that computes the same number, and say what depended on them agreeing." Evidence, one instance: before `510a280` the loupe's head was `Math.min(...allHits)` over every hit rectangle in the system and the window's left is `Math.min(...own)` over the held measure's (`loupe.ts:79`); the measure's set is a subset of the system's, and a subset's minimum is never smaller, so `head <= windowLeft` held by set containment and the two viewports could not overlap. **No comment said so.** Moving the head onto music ink killed the containment argument silently. Sub-points drafted: grep for the quantity rather than the identifier, since the two sites shared no name; if two things were equal and now are not, treat the difference as a defect until measured; leave the invariant in the code; and in a memo, put quantities that meet in one table, because two numbers four pages apart are two numbers nobody compares. **HELD because it would be a twentieth tether in a session that began by diagnosing that this project's governance accretes and never retires, and because the desk has ONE instance of the class.** Reconsider it when the retirement pass runs and names something to remove. The desk's proposed one-in-one-out rule is also unruled.
- N.105 (numbered by Dann 2026-09-01, unplaced): the install prompt never saves a dismissal, so "Not now" lasts exactly one page view. `InstallPrompt.svelte:73-77` guards its only write behind `if (isIos)`, so on desktop `dismiss()` sets `visible = false` and stores nothing; the next load fires `beforeinstallprompt` again and `:61` shows the banner eight seconds later. The iOS path writes `sessionStorage`, which dies with the tab. **This reaches singers, not just the walk**: anyone on the stable URL who declines is asked again on every load, in perpetuity. Dann hit it ten times in one hour on the 510a280 walk. Fix direction: persist the decline durably and per-origin, and rule how long a decline lasts before Ilya may ask again. NOT ESTABLISHED: whether a declined native Chrome prompt (`install()`, outcome `dismissed`) should count as a decline too.
- N.101 (numbered by Dann 2026-08-26, unplaced): score intake is confusing, and its next user is Dann himself. The evidence, all from tonight: an upload that renders is not saved until accepted, and an un-accepted upload dies on reload (ScoreUploader.svelte:45, +page.svelte:1267) with nothing saying so; New song against upload-into-active-song is an invisible choice; auto-names ("Untitled, date (n)") make a page-read song and an engraved song indistinguishable twins in REPERTOIRE; the clef was the only fingerprint that told them apart. The item: make intake legible to a new user, states named, nothing silent.
- Barline correction (desk, 2026-08-27, from Dann's slice 3 walk): the Lamm read fuses measures (m. 1 reads 29 of 8; missed barlines, the N.95 weak channel), and the entry grammar has no barline verb, so a singer can repair every note but cannot split a fused measure. Wants a number and a slice.
- Where did the Lamm read's metre come from (Dann, 2026-08-27, mid slice 3 walk): the tag counts in eighths, so the read's signature is presumably N/8. Which glyphs timesig.py matched on this plate, or what default fired, is NOT ESTABLISHED. One Code measurement answers it; rides with the barline-correction number.
- Arrow-key hotkeys for the stepper (Dann, 2026-08-27, mid slice 3 walk): on desktop, map keyboard left and right arrows to the stepper, Speedy's own grammar coming home. Belongs to slice 4, the desktop slice.
- Redo (Dann, 2026-08-27, mid slice 3 walk): a named Redo pill beside Undo, present only immediately after an undo, same reserved row, same sentence grammar. Wants a number; small.
- N.102 (numbered by Dann 2026-08-27): courtesy accidentals. Ilya has none today: staff-renderer.ts:963 draws an accidental only when REQUIRED, so what the slice 2 walk read as a courtesy was a mandatory cancellation. Wants its own control, its own French, and parenthesis glyphs Maestro must be checked for; NOT a cycle on the natural cell, which changes pitch and whose "none" would suppress a required accidental. The natural-cycle ruling of 2026-08-27 is WITHDRAWN by Dann in favour of this item.
- Tie taper ruled 0.40 (Dann, 2026-08-27, from Code's rendered comparison of three candidates). Provenance: DANN'S EYE, not Gould. Gould rules 150-175 were excluded from the priors memo and the book is not on this machine, so the engraved thickness stays NOT ESTABLISHED; if the source is ever photographed, check 0.40 against it. Maestro carries no composable tie glyph (all 2,728 names searched), so ties stay drawn geometry: a filled two-curve outline, thickest at centre, tapering to fine terminals.
- FOR PRODUCT.md, ruled by Dann 2026-08-27: the squircle is a key germ of Ilya's visual vocabulary. Not decoration and not one marker's shape: it is identity, and it should govern where rounded forms appear across the product. Recorded here because the desk had not read PRODUCT.md this session and would not write it blind. Transcribe into PRODUCT.md next session, alongside the Calm Authority shape rule (three radii, no fourth) which it refines rather than contradicts.
- Augmentation dots are not drawn (Code, 2026-08-27, §24): the renderer has no `augmentationDot` and no `dots`; `staff-renderer.ts:1093` calls it future work. So the Corrections surface dot cell (ruled 2026-08-27 to cycle dot, double dot, none) may be storing a correction that never appears on the page. NOT ESTABLISHED whether the diff carries it and only the drawing is missing, or neither exists. One measurement answers it; wants a number if it is the former.
- N.103 (numbered by Dann 2026-08-27): measures do not re-space when ink is added, walking the dot on dae29f5: the renderer lays notes at fixed x from the read, so an augmentation dot is drawn into space belonging to the next note and collides with its accidental. Adding a dot, an accidental, or any widening correction wants the measure re-spaced, which is real engraving work (Gould on horizontal spacing, minimum distances, accidental columns) and NOT a marker or z-order problem. Wants a number and a slice.
- N.104 (numbered by Dann 2026-08-27): the page must represent measures with no voice content. A tacet stretch shows as a single consolidated rest with the number of silent measures above it, per Gould (VERIFY: the priors memo records rest geometry, pp. 34-38, as never photographed, so her multibar-rest rules are probably NOT held; report NOT ESTABLISHED and fall back on engraving convention rather than inventing). This is the honest resolution of the measure-number disagreement Dann found on 2026-08-27 (loupe read m. 3 on the visibly second bar, consistent with one piano-only bar before the voice enters): do not renumber the tag, complete the page. A singer cannot count an entry from a page that hides the bars they must count. CONFIRMED by Code 2026-08-27, so do not re-measure: the Bass part's <measure number="1"> carries 0 notes, 0 rests, 0 lyrics; the Piano's carries 5; the page draws hit ids m1 through m17 with no m0; the tag's number is copied verbatim from the source (musicxml-parser.ts:599, read at +page.svelte:1102), so the TAG IS FAITHFUL and the page is short one bar. Also carried: the first system declares data-system="0-3" while drawing source indices 1-4, so the loupe's systemIndexOf compares two scales that differ by that same bar; it reads correctly today and wants a look when N.104 changes what is drawn. Gould holds NOTHING on rest geometry (priors memo line 220, pp. 34-38 unphotographed), so the multibar rest's dimensions are either photographed or named as convention and recorded as convention, the way the tie's 0.40 was recorded as Dann's eye.
- N.106 (numbered by Dann 2026-09-02, waits behind N.102): collisions between the sung notehead and the turning-layer notehead a second apart. `staff-renderer.ts:1510-1523` already applies Gould 103/104 (one head width + 1.6 px, rising diagonal), so the heads touch as a chord's would; Dann reads the touch as a collision because the lavender head is another layer, not a chord member. His words: "a conditional rule to detect these collisions between layers of notation and resolve them in a way that is legible and visually attractive." Taste question for him when N.102 returns: keep Gould's touching pair, or give the turning layer its own clearance. He offers more Gould pages if needed; pp. 47-57 are already distilled in v7 rules 102-108, 125-127.
- N.102 increment 1c (Dann's heads-up 2026-09-02, not yet placed): courtesy accidentals on the TURNING layer too, parenthesized, in lavender. Increment 1 was sung line only on a desk default; Dann expects the turning layer to carry them. Same rule, same gap, own per-measure state (`turningAcc`). N.106's placement measures the turning unit from its leftmost ink, so the wider biglyph needs no change there.
- N.107 (Dann 2026-09-02, "critically important"): turning heads outside the stave draw NO ledger lines. His words: "Without ledger lines, the noteheads are meaningless." The sung line's ledger loop (staff-renderer.ts, before the accidental at about :1368) never ran for the turning block. Fix: the same loop in lavender at the turning head's x, spanning its head width. Paste written; runs after N.103 ships, not in parallel.
- I.N.110 candidate (desk, 2026-09-02, not numbered): the plausibility guard judges readings at capture only. Dann's profile carries [i] fR1 = 1063 Hz marked Provisional with no plausibility verdict (predates or escaped the guard), so it passes `buildVoiceProfileSnapshot`'s gate and every [i]-anchored derivation ([ɨ] 1451, [ɪ]) inherits it; the page draws [i] turning heads near B4. Fix direction: run `expectedF1`/the guard on stored readings at snapshot time (computed, never stored) so an unchecked anchor gets a verdict before it anchors anything. Dann's remedy today: Re-take [i].
- Clitic on its own note (Dann, 2026-09-03, on the N.108-2 walk of Without Sun no. 1): a vowelless clitic (в before бьющемся) is placed alone under a sung pitch. Dann's rule, restated: a vowelless word cannot take a pitch alone; it concatenates with the word that follows and shares its first syllable's note. NUMBERED N.111 the same hour; see STATE.md.
- RULED by Dann 2026-09-03, rides with the next N.108 paste: the drawer stays still on a tab change. His words: "the stability of the control panel is helpful while the background and GUI shift." Cause: the 175 ms tab slide is applied to .drawer-content as well as .main-content (Drawer.svelte:1148-1162, class from +page.svelte:2769). Remove the drawer's half, keep the paper's.
- One piece at a time (Dann, 2026-09-03, on the N.108-2 walk, recorded on his word, not numbered): "we need a way to strengthen the perception that Ilya is working on one piece at a time. At the moment I feel like we have score from one source and lyrics from another that may or may not match." Symptom seen the same minute: Metadata fields tagged from score survive Clear on the score (handleClearScore, +page.svelte:1779, drops the score but not the header fields it wrote). Design question about how a song binds poem, score, and metadata; the walk's own fixture-on-a-Mussorgsky-poem mismatch sharpened it.
- RULED by Dann 2026-09-03 on the N.108-3 walk, rides with the next Code paste: negative space between the Piece band and the Ilya banner. His words: "This will increase the illusion that the controls float. On my screen, simply moving the controls down a few pixels would be sufficient." DESK DEFAULT: a top inset on the drawer equal to the groups' 16 px side inset; desk and phone alike.
- RULED by Dann 2026-09-03 on the N.108-3 walk, rides with the next Code paste: every button shares the calibration ritual's rounded ends (CalibrationWizard.svelte:1739, border-radius 999px; outlined pill, filled lavender pill, and text link are the three shapes). His words: "The buttons shown here can form the template. Can we make other buttons share its rounded ends?" Drawer buttons are 4px today (RootPanel.svelte:847, :923, :1011). Code audits every button in the tree first; fields and frames keep their radii.
- RULED by Dann 2026-09-03 on the N.108-3 walk, rides with the next Code paste: the OCR camera icon (top right of the field), Choose a file, and Read a score from a photograph "all serve the same function. Please consolidate these and find a reasonable place for the user to perform their file retrieval." DESK DEFAULT: one Choose a file button under the field taking every kind; a picture asks in place, the poem or the score (the PDF's existing askKind extended to images: poem goes to OCR, score to the page reader); the camera icon and the photograph button go; their strings are marked unused, not deleted; the phone keeps N.70's no-accept rule on the one picker.
- Found 2026-09-03 while reading the N.111 spot in Dann's engraved Sunless 01 musicxml (Downloads): the IPA verse (lyric 2) carries the assimilation marker # as a syllable on note 21 (G3, tень), which pushes every IPA syllable one note late until note 44, where the fused ˈvʲbʲu absorbs the Cyrillic verse's standalone в. The file therefore carries two readings of notes 43 to 46: Cyrillic в·бью·щем·ся, IPA jɑ·ˈvʲbʲu·ʃʲːɪ·msʌ. Not numbered; Dann to say what the printed score sets there.
- N.111 finding by Code 2026-09-04, not fixed: a re-seated cell loses its punctuation (я, becomes я) because the slot queue's Cyrillic is unpunctuated. Inbox candidate for increment 3 or its own item.
- N.111 finding by Code 2026-09-04, rider for increment 3: after the seat the last note of the piece is undecided and, on a lyric-bearing score, undecided draws the file's stale text (ночь о ди но ка ка). DESK DEFAULT: an undecided note inside a seated run draws nothing; no melisma is created by Ilya (E.46). The engraving lost its final я off the end; Dann's file to fix.
- RULED by Dann 2026-09-04 on the N.111 walk of 7875892, for N.111 increment 3: Ilya seats a vowelless clitic with its host AUTOMATICALLY at ingest, no proposal, no Seat button. His words: "I swear to you: no vowelless word in Russian can carry its own duration. You are complicating things for the user with a situation that is impossible in music notation." Amends the desk default of brief r2 §5.3 (propose, do not apply). E.24 §6's limit stands: Corrections states what Ilya did in one sentence with Undo, and asks nothing.
- RULED by Dann 2026-09-04: the transcription overrules the file's underlay ONLY on a clitic fold; where they diverge for any other reason (a composer setting several syllables on one duration) Ilya withholds as today. His words: "I like your distinction, especially when we expand Ilya to process Italian text where multiple syllables on a single duration are common." Sent to Code as N.111 increment 3 with the automatic seat, the bare undecided note, and the hand.
- N.111-3 walk findings by Dann 2026-09-04 on c574cf8, both for Code: (1) the loupe does not redraw after a click seats a syllable from the hand; the page behind it updates (но – ка я, ka ja) while the loupe keeps the old cell (ka a, но – ка). (2) A blanked note draws a stray IPA 'a' in the loupe before the hand seats anything; the Cyrillic is bare as ruled but the IPA line is not.
- RULED by Dann 2026-09-04 on the N.111-3 walk: the Corrections sentence about the seated clitic and its Undo are removed. His words: "I'm not crazy about the idea of that courtesy warning we give about Ilya having concatenated a clitic... I think it's unnecessary." Ilya seats and says nothing. Amends the 2026-09-04 ruling that kept the sentence; E.24's 'show it' is satisfied by the page itself. Rides with the Finale lyric-controls item.
- Dann 2026-09-04, asked for: "revisit Finale's lyrics controls and provide whatever useful controls they do." Desk to search the project (E.46 N.55b click assignment design) then Finale's documentation, and bring a table.
- Dann 2026-09-04: "I'm also rethinking the idea of independent Transcribe and Continue to score markup actions. I think one should invoke the other." DESK DEFAULT reading: either action does both; Transcribe with a score attached runs the analysis, a dropped score with an untranscribed poem transcribes first. Not yet numbered.
- Finale lyric-controls review, desk, 2026-09-04, from the current Finale manual (menu-lyrics.htm, EDITLYRC.htm) against E.46's survey: two candidates, (a) the text is authoritative: after re-transcription seats whose word is unchanged keep their note, a removed syllable slides the rest left, no drift the singer did not cause (this is Code's 59-drift question); (b) a melisma control in the dock and an extender line on the page; the melisma kind exists in pairings.ts:121 and no control writes it. Not adopted: Type Into Score, Adjust Syllables, alignment/baseline/options, verses (D3's Job A), Clone. Numbering proposed to Dann as N.112 and N.113.
- N.115, numbered by Dann 2026-09-06: the singer moves a measure between systems. His words: "Finale offered a functionality in Page Layout where the user could alter the distribution of measures per system. In cases like our current file (the Mussorgsky songs) there is a lone orphaned measure at the end. In Finale, I would be able to select that measure and arrow-up, and the measure would force itself into the previous system, with the other measures respacing accordingly." He wants more singer control over page layout. Research Finale's Page Layout / measure-layout commands first (his ask), then check the existing orphan rule in the tree (N.103/N.104 era; find it) before scoping. Unplaced.
- Walk of d5a49ff by Dann 2026-09-07, for the paste after N.108-5 (same +page.svelte, do not run in parallel): (1) the loupe redraw fix HELD: the placing click changed page and loupe in the same frame. (2) RULED: the loupe stays open after a placing click; today it closes at once. His words: "I expected to see the change reflected in the Loupe first." (3) RULED: Undo and Redo on the loupe, for placements as well as shifts. His words: "we do not include an Undo/Redo button on the Loupe. We need one." (4) Observed: a second click on the same note seats the cursor's next syllable over the first (Ком became на); expected by the cursor's advance, made confusing by the loupe closing. (5) NOT walked: the bare-note IPA line, because the last note carries his hand-placed я.
- RULED by Dann 2026-09-07: four groups, Piece · Input · Text · Score markup. Input is the whole intake as its own band, painted SAGE like Text (his words: "since the Input is about text it doesn't need a fourth colour"). Sent to Code as a rider on N.108-5.
- RULED by Dann 2026-09-07: the button reads "Transcribe and fit" (vocal fit, not page fit); Process rejected as vague, Prepare offered and not taken. French owed. Sent to Code as a rider on N.108-5.
- N.114 frame, agreed 2026-09-07 (Dann's idea, desk-adjusted): the syllabified line moves from Score markup to sit under the field inside the Input band, live on text present, collapsed by default; without a score it is a read-only syllabification (hyphens, no box); with a score the box appears and a click moves the cursor. Score markup keeps Corrections, Voice, and a placed count. The loupe dock keeps the hands: taken note, next syllables, shifts, Undo and Redo, N.114's typed field. Finale's two surfaces, E.46. Drawing before Code.
- RULED by Dann 2026-09-07: "whenever text is present, the transcription exists." At boot with text, on paste, and after a typing pause, Ilya transcribes; the button stays as Transcribe and fit for now, its removal a later ruling. Closes the N.67 4b boot asymmetry. Sent to Code as the third rider on N.108-5.
- N.108-5 findings by Code 2026-09-07, both open: (1) an implicit transcription (boot, paste, typing pause) resets the singer's stress overrides, ё toggles, and syllable divisions, because they key on word position; existed on every Transcribe press before, now happens on every edit; keeping marks across an edit is N.112's. (2) RootPanel.svelte carried an unterminated CSS comment swallowing .char-warning and .error-text since early September; moved verbatim into IntakePanel.svelte, still broken; one-line fix for the next paste.
- 2026-09-08: the unterminated CSS comment in IntakePanel.svelte (memo-n108-5 §7 item 11) is closed by Code, one */ at :720, verified in the browser; UNCOMMITTED, rides with the next ship.
- RULED by Dann 2026-09-07: the intake's receipt tag reads POEM, not TEXT (input.watermark's English changes; the French « texte » is owed a decision, likely « poème »), so it no longer shares a name with the TEXT band; SCORE stays. Rides with the next paste.
- RULED by Dann 2026-09-07 from a print preview: the printed page is white; no cream regions on paper. Cause found: staff-renderer.ts:2740 paints a per-system #F0EBE0 rect; stripBackingRect (VoiceProfilePane.svelte:662) strips only the white page rect. This is the 'stripBackingRect matches #FFFFFF while staff-renderer paints #F0EBE0' item from STATE's STILL UNSETTLED list, now settled. Sent to a second Code thread (different files from N.112).
- N.112 walk defect, Dann 2026-09-07 on b191867: replacing безответная with бесконечная in line 2 seated the new word on system 1's notes 2 to 6 (Ком – бес – ко – неч – на – я) while system 2 kept без – от – вет – на – я. The insert took notes after the wrong anchor; Code's own walk of the same edit read indices 19 to 23 and passed, so the difference is the alias's state (a hand-placed Ком at note 0 with its own origin, and the 4 kept placements with no note). Transcription page updated correctly.
- N.112 walk finding 2, Dann 2026-09-07 on b191867: after Start placement over, the last note of the piece is bare (одинокая ends on ка); the rebuild seated 95 on 96, so its queue lacked the poem's я. Which queue the rebuild used is NOT ESTABLISHED. Sent to Code with the anchor defect.
- Dann 2026-09-07: "We need to update GUIDE and LEARN to reflect the new design(s) and functionality." DESK DEFAULT: Learn joins N.84 (the Guide redo) as a second pass under the same rule (no claim outruns the build). Placing asked: after N.114 as placed, or pulled forward.
- N.116, Learn as the book, numbered by the desk on Dann's ask 2026-09-07, UNPLACED. His words: "Years ago Grayson agreed to co-author a book with me based on his dissertation. Instead, I developed Ilya. To my mind, LEARN is ideally that book. Can we work on a LEARN re-structuring to capture all of whatever's great in Grayson plus whatever I added in the current Learn, and review the sequence that we offer to make sure it is suitable for singers who need to know a little more about how Russian works?" Rule set by the desk: every chapter from Grayson is cited as his; Dann's additions marked as his. Step 1 an inventory (Learn chapters in the tree, Grayson's TOC from the PDF), farm-out flagged; step 2 the desk proposes a sequence for his eye.
- N.116 step 1 DONE 2026-09-07: docs/sessions/inventory-n116-learn-grayson_r1_2026-09-07.md (Sonnet, 186k tokens) and docs/sessions/n116-dann-lit-review-sung-russian_2026-09-07.md (Dann's dissertation § A.1.2, verbatim), both untracked. Step 2, the desk's proposed sequence, needs the inventory read in full and Grayson chapters 1, 8, 9 read; not started.
- RULED by Dann 2026-09-07: a note the singer's own edit vacates draws nothing (not the file's old word). His words: "it draws nothing, because the singer removed the word. Yes." Closes memo-n112 §8. Rides as rider 0 on N.113.
- Walk of e1bcb67, Dann 2026-09-07: the dock's Melisma pill sits on the two shift-arrow pairs with no gap, and the arrow pairs touch each other. Fix in the next Code paste: the gap between the Melisma pill and the arrows, and between the arrow pairs, matched to the dock's other rows.
- RULED by Dann 2026-09-07: on the desk, Cmd-Z (macOS) / Ctrl-Z executes Undo as a hotkey. DESK DEFAULT: Shift-Cmd-Z / Ctrl-Y is Redo; neither fires while a text field has focus, so the poem keeps the browser's own undo. Rides with the dock-spacing fix.
- Walk of e1bcb67, Dann 2026-09-07: the loupe marks the taken note with a vertical bar drawn AFTER the notehead, which reads as an insertion point between notes (his words: "misleading because the insertion point was in the space after тес"); the page marks it with a box on the notehead. Fix in the next Code paste: the loupe marks the taken note the way the page does.
- RULED by Dann 2026-09-07 on the N.113 walk, reversing the desk's N.112 default (Finale's slide-left, never put to him): deleting a word from the poem vacates its notes and moves NOTHING else; an inserted word takes open notes only and never pushes the tail. His words: "I expected the mnogo to disappear without affecting the other text underlays." For the next Code paste.
- N.113 walk defect, Dann 2026-09-07 on e1bcb67: after a melisma shift had pushed one syllable off the end, deleting много left the last note reading ка ка on both lines: the vacated tail still draws the file's word in this state, though Code's clean-state walk of rider 0 passed. Reproduce with a melisma set before the deletion. Same paste.
- RULED IN AS N.117 2026-09-07. Dann 2026-09-07 late, on the N.113a alias walk: the page takes a few seconds to load on reload; consider a placating message so the singer knows Ilya is loading. Unnumbered.
  - Same night, second mention: Dann's preference is a PROGRESS BAR for the load, not a message.
- [2026-09-09] Loupe's taken-note box reads weird (tall) on the dotted B♭3, Without Sun no. 1 m. 2; Dann: adjust it. See VoiceProfilePane.svelte:459 height rule.
- [2026-09-09] Loupe locator line 2: Dann wants "dotted quarter" (and the double-dot form) instead of "Quarter · Dot". French to be shown to him.
- [2026-09-09] Dann: the Open syllables toggle (Text > Notation) should alter Score markup live, as it does the Transcription. Tree fact from STATE: the toggle clears syllable overrides (+page.svelte:2391).
- [2026-09-09] Phone-only idea, unbuilt: a Lyric-station verb that jumps to the poem box with the SABB's word selected (the hunt from note to word). Only if a phone walk shows the hunt costs something.
- [2026-09-09] RULED by Dann on the N.114 alias walk: the Voice station in Score markup stays expanded always; strike its chevron. Reason, his: Calibrate/Re-calibrate opens its own drawer surface. Small, unnumbered, Code's next pass over +page.svelte's stations.
- [2026-09-09] RULED by Dann on the N.114 alias walk: the intake hint (`intake.dropHint`, approved copy, unchanged) moves to sit directly under the textarea, above the receipts, as the field's caption. Choose a file stays where it is. Glyph rejected; state-dependent caption rejected. Rides with the Voice-chevron pass.
- [2026-09-09] RULED by Dann: Undo and Redo move to the top bar, right end, fixed, pill sentence as label; one undo, one place; dock row goes. In brief-n114a item 3.
- [2026-09-10] RULED by Dann: Undo/Redo pills align to the drawer's right edge, above the hands (N.114b, briefed).
- [2026-09-10] RULED by Dann: Start placement over moves into the syllable line's open header row; pill under Voice goes; air under every band 0.35rem (N.114b items 2 and 3).
- [2026-09-10] RULED by Dann: Start placement over is a ghost pill in the open syllable line's row (N.114b item 4). Undo clause for the rebuild still owed, English and French.
- [2026-09-10] RULED by Dann: Export and import buttons ordered Export all songs, Export this song, Import a song (N.114b item 7).
- [2026-09-10] For N.84 (Guide and Learn redo), Dann's ask: explain Revert to score header. A score file carries its own header (title, composer, opus, poet, translator) and Ilya fills the Piece fields from it, tagging each "from score"; a hand edit drops the tag; Revert restores the file's values verbatim and leaves untouched any field the file never had (`+page.svelte:3512-3518`). Users may not know text lives inside a music file; say so.
- [2026-09-10] RULED by Dann: the Notation toggles (Text > Notation) must change Score markup in real time as they do the Transcription. Numbered N.119 (DESK DEFAULT). Tree fact from STATE: the open-syllabification toggle clears syllable overrides (`+page.svelte:2391`); the IPA line under the notes comes from `underlayResolvers` (`VoiceProfilePane.svelte:748-755`), which takes `openSyllabification` only. Code reads which toggles reach it and which do not.
- [2026-09-10] RULED by Dann (N.121, the two sets of words): when a score arrives before a poem, Ilya ASKS, in a short prose sentence with context, then a yes/no pair, whether to use the words that came with the file or keep only the melody and enter the poem later. His sketch, NOT the text: "Would you like to use the lyrics that come with your file, or simply extract the melody and enter the poem later?" Terse verbs ("Use the score's words") rejected. Desk to draft the sentence and both answers, English, then French shown to him. Pattern: the same in-place question the PDF already asks (`intake.pdf.*`).
- [2026-09-10] RULED by Dann on Design's reply: (1) lyric hands FIRST in Corrections, always, fixed order, no swap by state; (2) strike the METADATA label on the Piece band (DESK DEFAULT that follows: the metadata body is always shown when Piece is open; collapsing Piece is the collapse); (3) on a phone, a step's primary action lands the singer on the page and Back brings them home (read against portrait C before Code). The ⓘ on Russian-o: Dann asked why Design dropped it; answer below; not ruled.
- [2026-09-10] USAGE, Dann's screenshot: Fable 62% used, All models 38%, both reset Sunday 05:00; session 56%. Fable is the scarce pool this week: farm mechanical work to Sonnet/Opus.
- [2026-09-10] FRENCH TABLE shown to Dann for the path strings (Design reply + desk): Calibrate / Calibrer (adopted from Recalibrer); 37 / 94 placed / 37 / 94 placées; 2 of 7 changed / 2 sur 7 modifiés; Grayson defaults / Grayson par défaut; 2 notes corrected / 2 notes corrigées; Type or paste the poem. / Saisissez ou collez le poème. (saisir and coller adopted, tree's own); Drop the score here. / Déposez la partition ici. (déposer adopted); Voice: Dann · 10 of 10 / Voix : Dann · 10 sur 10. Dann approved « saisir » explicitly ("that's what I was reaching for"); the rest of the table not yet ruled line by line.
- [2026-09-10] RULED by Dann: the ⓘ on the Russian-o roster row stays, as the ONE NAMED EXEMPTION to slate rule 11 (siblings behave identically); it opens the sung-[o] Learn note, ruled 2026-07-11. Design's removal rejected. Record the exemption in PRODUCT.md with the grammar.
- [2026-09-10] RULED by Dann: no word on every unplaced syllable ("nothing means anything when everything is annotated"); the count plus black/grey carries it on the page; per-syllable state goes in the accessible label only.
- 2026-09-10 (desk, from the anchors memo): French owed on untranslated keys: group.piece, group.input, group.text, group.scoreMarkup, binder.heading, voice.heading, loupe.station.corrections, intake.dropHint, intake.choose. Show Dann the table before any is written.
- 2026-09-10 22:12 (Dann, mid-walk): the running header on page 2 and after should read Composer - Title, not the title alone, unless the desk has a better form; he is also thinking about how to name the files so a growing library stays organized. Unruled.
- 2026-09-10 23:05 (Dann): review how Finale handles tempo, with special attention to gradual text cues (ritardando, rallentando, schnell, and the rest) that imply a smooth rate change rather than a binary flip (attacca); music blurs the distinction and Ilya should plan for it. Prior work: claude/fit-tempo-cue-lane-spec_2026-07-17.md (music21 linear ramps adopted; denigma emits only point bpm at downbeats, no verbal or gradual text). Finale's own interpretation (Human Playback, expressions) NOT ESTABLISHED as researched. Belongs to N.120's Tempo station.
- 2026-09-10 23:58 (Dann, seen on the alias): the page-2 running header already reads COMPOSER | POET — TITLE (Kabalevsky | Shakespeare — Бог Купидон дремал в тиши лесной). The 22:12 ask above is satisfied by the existing build; the earlier view had the Piece fields still empty. No work owed. The file-naming half of that line stays open.
- 2026-09-11 00:07 (Dann, on the Kabalevsky T05 Score markup page): two ties/slurs are 'terrible and artless', nearly flat and thin, one mid page 1 and one near the foot of page 2, where the short ones are graceful curves; both ugly ones are the long spans (one crosses a barline). Cause NOT ESTABLISHED: the renderer's tie/slur curve code not opened tonight. Also: measure numbers on Score markup are wanted; the bar-numbers drawing (32 plates, 2026-08-29) is WAITING ON HIS RULING of size, weight, clearance, post-rest anchor, bare vs parenthesized (STATE.md, RULINGS DANN OWES).
- 2026-09-11: the advice programme (tier-1 mining list led by Bozeman; Dann's dissertation Appendix A extracted to a file this evening; the open question is which sources Dann can supply as pages or PDFs).
- 2026-09-12, Dann's screen on `9026a56`, alias, his own profile: the poem box is EMPTY (placeholder showing) while the syllable line reads `71 / 95 placed` and the paper says `Enter your Cyrillic text in the drawer on the left.` Placements survived, the poem did not. Song: Without Sun no. 1, score `from score`, Voice Dann, 2 notes corrected. NOT ESTABLISHED, not touched by increment 3. Ruled in by Dann for investigation next.
- 2026-09-12, WALK FINDING against N.121, not a digression, fold into STATE.md at close. Dann's words, on the empty poem box beside a fully drawn score: "You are hitting on the confusion I have felt between input sources. Yes." He affirmed the desk's reading that the drawer contradicts the page and that the empty field's invitation to paste sits above 71 placements. Desk's ranked designs on the table, unruled: (1) the field holds the poem on arrival, placeholder only when there is genuinely no poem, per N.112 text-is-authoritative; (2) if the poem text is not stored and only the syllables are, the field is the wrong shape and a `POEM · from score` receipt with Clear and Replace replaces it; (3) do nothing, rejected, it reached him on his own library. Next design pass on the INPUT band belongs here.
- 2026-09-12, AMENDS the STATE.md entry of 2026-08-29 under RULINGS DANN OWES ("the loupe draws the singer's words in a different typeface than the page does"). Mechanism established by reading tonight, not a container override as that entry says: the renderer's own SVG root declares `font-family="'Source Serif 4', Georgia, serif"` (`staff-renderer.ts:2827`), and `paginateScore` STRIPS that opening tag when it nests each system (`page-layout.ts:376`). The page's own root (`page-layout.ts:364`) and system wrapper (`:374`) declare no font, so the Cyrillic text (`staff-renderer.ts:2697`, no font of its own) inherits `body`'s `var(--font-sans)` (`app.css:200-201`). The loupe re-declares the serif on its wrapper (`Loupe.svelte:884`, `:897`), so it keeps the renderer's intent. THE PAGE IS THE DEVIATION, and nobody chose sans. Fix is one attribute on `page-layout.ts:364`. Which face the Cyrillic wears is Dann's, unruled.
- 2026-09-12 (Dann, mid-walk, ANSWERED same session, keep for the Guide N.84): flagging and barring both appear to be present on the Paper GUI; what does Gould say, and why are we not aligned with her? Sources to open first: `claude/gould-beams-delta-pp16-25_2026-08-18.md` (project knowledge, Gould rules 245-284, Ground Rules pp. 16-25, two independent readings, ONE UNRESOLVED contradiction on p.18's three-beam rule) and `claude/ruling-semantic-stems-vs-gould-priors_2026-08-18.md` (Dann's ruling: an engraving convention is a PRIOR, not a law).
- 2026-09-12, RULED BY DANN: the drawer band `SCORE MARKUP` becomes `MARKUP`, to end the collision with the desk-head tab of the same English name (flagged in the tree at `i18n.ts:49-54` as "DANN'S TO RULE"). The tab keeps `Score markup` / « Partition annotée », ratified 2026-08-19. One string, `group.scoreMarkup` (`i18n.ts:59`), whose French is OWED (it currently reads `Score markup` in both languages, a placeholder). FRENCH NOT RULED: the desk proposed « Annotation », coined 2026-09-12 from the ratified « Partition annotée ». Cannot go to Code until Dann rules the French.
- 2026-09-12, MEASURED by the desk in the built-in browser on `9026a56`, branch alias, 390x844, fresh profile. The desk head at 390 px: available width 342 px (24 px gutters). ENGLISH: Transcription 116.05, Score markup 113.03, Learn 37.48, Guide 36.15; nothing overflows, about 24 px of slack. FRENCH: « Partition annotée » does not overflow, it WRAPS to two lines inside its card (element height 41.59 vs the links' 18), which stretches the Transcription card to match; slack falls to about 17 px; Learn is « Leçons » 45.46. THIRD TAB: `Insights` measures 78.45 px in the same style. English pair plus Insights = 307.53 + 16 gap + 88.03 links = 411.6 against 342, over by about 70 px. French is worse. CONCLUSION: N.127's third document tab does not fit at 390 px as the row is built today. Desk's ranked directions, unruled: (1) move Leçons and Guide off the row on a phone, freeing about 104 px, which is what a third tab needs; (2) shorten a tab word, which saves only about 41 px and is not enough alone; (3) horizontal scroll, rejected, a hidden tab is a tab nobody finds; (4) wrap to two rows, costs vertical space where it is scarcest; (5) icons, rejected, Ilya's grammar is words. ALSO SEEN, consistent with the French-owed list: `paper.empty.mobile` prints in English in a French session ("Tap Drawer at the bottom of the screen to open the drawer.").
- 2026-09-12, RULED BY DANN, French for N.127 Insights: « Aperçus ». His words: "Aperçus is perfect! I love it". English `Insights` stands. No coinage: every candidate considered was an ordinary French word. Measured width in the tab's own style at 390 px: 76.25 px, narrower than `Insights` at 78.45. REJECTED, with reasons, so nobody re-opens it: « Infographie » names a visual form Ilya does not make and is design jargon; « Détails » and « Précisions » both frame Insights as subordinate to Score markup, against the ruled sibling status, and Détails implies completeness against "helpful not comprehensive"; « Explications » collides in purpose with `Guide` and « Leçons ». « Éclairages » was the named fallback if the Apple « Aperçu » / Preview association had bothered him; it did not. Fold into the N.127 record and STATE.md at close.
- 2026-09-12, DEFECT, NUMBERED N.128 by Dann the same evening and promoted to STATE.md; brief written at docs/sessions/brief-n128-stale-beat_r1_2026-09-12.md. A CORRECTED DURATION DOES NOT MOVE THE FOLLOWING NOTES' STORED BEAT, SO BEAM GROUPING GOES STALE. Found on Dann's own Sunless 01 page, system 1, where « на–я » is flagged and « ла–я » is beamed although both are drawn as two eighths in the last beat. MEASURED from the live SVG in Dann's browser: spacing identical for both pairs (тес→на 54.78 px = ми→ла 54.78 px; на→я 20.14 vs ла→я 19.67; я→rest 15.68 vs 15.69); all four stems down, so timbre did not break the group; the only eighth rests (SMuFL e4e6) sit at x 353.2, 490.85, 602.5, each AFTER the second « я », so no rest or barline falls between either pair; the system's single beam runs x 563.89 to 583.56. CAUSE: the beam key is `measure | beatIndexOf(ev.rhythmicPosition.fraction, ts) | timbre` (`staff-renderer.ts:1611`), and `modification-engine.ts` has ZERO references to `rhythmicPosition` (grep, 0 hits). A duration correction changes the duration and the x layout advances by duration, but no following event's rhythmicPosition is recomputed. On the page на is event `m1-1-1` (1/1 whole = 4.0 quarters, beat 3 of 12/8) while я is `m1-5-4` (5.0 quarters, beat 4): different beats, no beam. The other pair is `m2-9-8` (4.5) and `m2-5-4` (5.0), both beat 4, so it beams. Corroborating: the source file (`~/Downloads/Mussorgsky - Sunless 01 - Within Four Walls (engraved).musicxml`, md5 265f7cb5fa359942b54826795cf10c4f, byte-identical to `apps/web/src/lib/shane/ingestion/fixtures/sunless-01-engraved.musicxml`) has тес as a PLAIN quarter at 3.0q, while the page draws it 1.5 quarters wide. WIDER SCOPE, NOT VERIFIED: two other consumers compare the same stale field, `sustain.ts:67-79` and `watchlist.ts:232-240`, both matching a note against a tempo or marking position, so a corrected duration may mis-assign a sustain marking or a watch-list entry as well.
- 2026-09-12, FINDING from Code's loupe-typeface memo, DESERVES A NUMBER (desk recommends N.129, Dann to rule). THE SCORE'S UNDERLAY IS SPACED IN THE WRONG FONT'S METRICS. `underlay-widths.ts:690` declares "Per-1000-em advance widths for Source Serif 4 Cyrillic (measured at 12.5px)", and the renderer uses that table for the syllable column advance (`staff-renderer.ts:754-762`) and for hyphen and extender endpoints (`:2742-2750`). The page has drawn those glyphs in Source Sans 3 since the paginator began stripping the renderer's serif root (`page-layout.ts:376`), so every syllable's spacing and every hyphen/extender end on Score markup has been computed from metrics the glyphs never had. Code measured the gap at about 5% on one word: « ночь » 27.72 px serif against 26.34 px sans. Desk read `underlay-widths.ts:690` and both renderer sites itself, 2026-09-12. Tonight's loupe change did NOT create this and does not worsen it: the loupe clones the page's layout rather than re-measuring, so there is one error, in the layout, not two. Candidate fixes, unruled: remeasure the Cyrillic table in Source Sans 3, or make the face a parameter so the table and the drawn font cannot diverge again. Bears directly on N.118 (a trailing comma inherits the same error) and on the `columnAdvance` crowding item already in STATE.md's OWED list.
- 2026-09-12, TWO DESK ERRORS IN `brief-loupe-typeface_r1_2026-09-12.md`, caught by Code and owned. (1) The brief asserted that `routes/fit-font-lab/+page.svelte` "never touches the score SVG"; it does, inserting the renderer's whole SVG at `:84` and `:94`, so that dev page's unnamed text now draws sans. Not opened in a browser by anyone. (2) The brief's dependency check missed the Source Serif 4 width table entirely, which is the finding above. The desk had read `underlay-widths.ts` earlier the same evening for N.118 and did not notice the font named in the comment.
- 2026-09-12, SMALL RULING OWED BY DANN: Code ran git and said so unprompted. The commands were read-only (`ls-files`, `check-ignore`, `status`, `diff --stat`); nothing was staged, committed or changed, and the memo discloses it. CONTRACT §5 says "YOU DO NOT RUN GIT. AT ALL." Dann to rule whether that means no git at all, or no state-changing git. The rule exists because of the 2026-08-20 `git add -A` sweeps of the shared working tree, which were writes.
- 2026-09-12, CAUSE ESTABLISHED for the empty poem box beside a fully drawn score, and it closes the walk finding recorded earlier this evening. **N.121's ruling of 2026-09-10 is NOT BUILT.** That ruling: "A score arriving with words into an EMPTY box fills the box and tags the poem receipt 'from score'." Read by the desk 2026-09-12: `doc.inputText` is assigned in exactly three places, `+page.svelte:2679` (the field's own input), `:2403` (clear), and `document.svelte.ts:269` (restore from the stored record). **No path anywhere fills the poem box from an ingested score's lyrics.** The stored record does hold the poem (`library/types.ts:80`, restored at `library/library.ts:65`), so the storage is not the gap; the poem was never written in the first place. The syllables on Dann's page come from the score file's own underlay, which is why Score markup is full while the poem box and the whole Transcription document are empty: Transcription draws from `lines`, built by transcribing `doc.inputText`. Dann's words, 2026-09-12: "If there is text, it should be fed through the Transcribe pipeline, and we might as well capture it as a formatting Transcription." THE HARD PART, and it is why this may have stalled: a publisher's underlay is syllabified (Ком, нат, ка as separate seats), so filling the box means rejoining words rather than pasting the syllable stream. Feasible: the file's own division is parsed and available as `ev.syllable?.type` (`pairings.ts:713-715`), so `begin` through `end` concatenates. Brief to be written under N.121, not a new cardinal.
- 2026-09-13, PARKED at Dann's word, waiting on him, nothing owed by the desk. ONE FOOTER FOR THREE DOCUMENTS. He found the three feet inconsistent after Insights shipped. Divergences established by reading: `PageFooter.svelte` (Transcription, Score markup) puts a 0.5 px rule in the page's accent ABOVE the block and sets the page number in its own grid column on the attribution's last line (`:170-171`, `:176-180`, `:216-226`); `InsightsPane.svelte` puts a 1 px `--dusty-rose` rule BELOW the apparatus and spends a whole line on the page number (`:708-709`, `:713-719`). Drawing with six plates, rendered and checked: `docs/sessions/drawing-one-footer_r1_2026-09-13.html`. Desk's recommendation, unruled: proposal A, one geometry everywhere, matter above a 0.5 px accent rule, page number back on the attribution's last line; measured 99 px today against 90 px under A for Insights. The real lever, also unruled and Dann's alone because it is a licensing decision: cutting the attribution from three lines to two takes the block from 69 px to 53 px. Design not contacted.
- 2026-09-13, SELECTED BY DANN, NOT ADOPTED. A warm brown for the colour story, should one ever be wanted. His words: "Umber is the best selection... I'm not sure whether I want to use it or not, but I wanted the option." NOTHING IS ASSIGNED TO IT and no destination uses it; it is on the shelf. **UMBER, OKLCH hue 65°, built exactly as the four existing families are built:** light (desk) `#D7CCC0`, medium (band) `#9C7F62`, dark (chip, one step down) `#8E6F51` at 4.62 on white, and the fifth value rose also needed, label ink for small caps on cream `#7A5D40` at 5.09 (lighter alternative `#806245` at 4.71). Proposed names, unruled: `--warm-umber` follows the two-word pattern, `--quiet-umber` echoes cobalt's adjective. ESTABLISHED while deriving it, and worth keeping: **the light tier is literally 40 % hue plus 60 % white**, verified against all four families, every hex reproducing exactly; the dark tier was hand-set per family to clear 4.5 on white, which is why no single multiplier fits it. Existing hue angles for spacing: rose 19°, sage 130°, cobalt 262°, lavender 310°; umber at 65° is 46° from rose and 65° from sage. Rejected candidates and why: chestnut 45° reads as a warmer rose at only 26° from it; tobacco 75° starts to read olive. Drawing with all four candidates, the reference families, and umber in situ: `docs/sessions/drawing-warm-brown_r1_2026-09-13.html`.
- 2026-09-13, RULED BY DANN: the colour families take one-word names. **sage, rose, lavender, cobalt, umber.** So `--dusty-rose` becomes `--rose`, `--deeper-lavender` becomes `--lavender`, `--quiet-cobalt` becomes `--cobalt`; `--sage` is already right and does not move; umber joins the set if and when it is adopted. He chose "rose" over "pink" when the desk noted that `#A67B7B` is a muted brownish rose and "pink" would set a brighter expectation. NO HEX VALUE CHANGES. Counted by the desk 2026-09-13: `--dusty-rose` 25 uses in 12 files, `--deeper-lavender` 49 in 22, `--quiet-cobalt` 21 in 15, about 95 call sites. Tests assert hex literals, not token names, so no gate can break from the rename alone. DESK DEFAULT, unopposed: the pacifier's `--muted-lavender` and `--light-lavender` stay as they are, because they name values inside a family rather than families. Brief: `docs/sessions/brief-colour-token-rename_r1_2026-09-13.md`.
- 2026-09-13, RULED BY DANN. **THE COLOUR STORY, SETTLED. Transcribe into PRODUCT.md at the next touch.** The principle, in his framing and corrected by the tree: **a hue names ONE IDEA, and a band takes the hue of the idea it holds, wherever that band appears.** A hue travelling into another room is not a collision; it is the system working. Evidence, read 2026-09-13: `Drawer.svelte:1187` paints the Input band with `var(--lang-chip-transcription)` and carries Dann's own N.108-5 ruling in the comment, *"painted sage like Text (hue names place: it is text)"*, so sage already travels into Score markup's drawer and always has; `:1178` paints the Piece band with `var(--lang-chip-guide)`, so cobalt already travels too. THE DESK'S EARLIER CLAIM THAT COBALT-ON-PIECE WAS "THE ONLY GENUINE COLLISION IN THE SYSTEM" WAS WRONG and is withdrawn; it proposed a neutral Piece band on that wrong basis and Dann was right to resist. THE MAP: **sage = the words** (Transcription, the Input band); **lavender = notation and voice** (Score markup, its band, the turning layer, the pacifier); **cobalt = information** (Guide, the Piece band) — Piece STAYS COBALT, ruled; **rose = your voice, explained** (Insights); **umber = the book** (Learn). THE ONLY MOVE THE REFORM REQUIRES: **Learn goes from rose to umber**, because rose was carrying two unrelated ideas, which is why `--surround-insights` had to be minted as a second name for `#DBCACA`. Everything else stays where it is. Umber's values are in the 2026-09-13 umber line above, pending the four-value ramp decision. SEQUENCING, desk's note: the band/chip/ink spacing proposal (steps of 0.11 instead of 0.05) is NOT ruled, and umber's three marks depend on it, so ruling the ramp before building Learn's umber avoids building it twice.
- 2026-09-13, RULED BY DANN, two small ones. (a) The French for the drawer band `MARKUP` is « Annotation », SINGULAR. Adopted, not coined: it is the noun the ratified tab name « Partition annotée » (2026-08-19) is built from, so the French pairs as the English does, the band naming the act and the tab naming the result. The band rename can now go to Code. (b) CONTRACT §5's git rule is NARROWED and ratified: no agent WRITES with git (add, commit, push, checkout, reset, restore, clean, stash, rm, mv, merge, rebase, tag), read-only git is allowed (status, log, diff, show, ls-files, check-ignore). Written into CONTRACT.md the same evening with the 2026-08-20 `git add -A` incident cited as the reason. Code's disclosed read-only git use on 2026-09-12 was therefore not a breach under the narrowed rule.
- 2026-09-13, DANN'S BRAINSTORM, three parts, NUMBERS OWED. (a) **The drawer band `Score markup` is renamed `Melody`.** This SUPERSEDES the 2026-09-12 ruling that made it `MARKUP` and makes « Annotation » moot: the band now needs its own French, not the tab's noun. The tab keeps `Score markup` / « Partition annotée ». The band-versus-tab collision is gone rather than narrowed, because Melody names a different thing. (b) **Corrections gains a sibling, `Transposition`**, inside Melody, modelled on Newzik's interface, NOT BUILT. Dann: number it. Note: N.94 in `INBOX.md:45` is already "transposition interface, modelled on Newzik" from 2026-08-24, so this may be N.94 returning rather than a new cardinal; Dann to say which. (c) **`Voice` extracts from Score markup to become a band sibling of Piece, Input and Melody**, with its own signature colour, and the whole calibration secondary interface recast in it (the vowel table header, the Re-take pills, Finish, Start over, Add voice characteristics). Screenshot supplied.
- 2026-09-13, OPEN CONFLICT for Dann, arising from the brainstorm. Dann assigned UMBER to the new Voice band, hours after ruling umber = the book = Learn. Under his own principle a hue names one idea, so one must give. Desk's reading, unruled: **Voice is ROSE**, because rose already means "your voice, explained" (Insights); calibration captures the voice and Insights explains it, one idea in two rooms exactly as sage sits on the Input band. Umber then stays with Learn and lavender simplifies from "notation and voice" to just "the melody", which is the band's new name. The alternative is umber = the voice, which reopens the Learn-and-Insights tangle closed earlier the same evening. Dann's eye decides.
- 2026-09-13, RULED BY DANN, THE COLOUR MAP CLOSED. **Voice is ROSE.** His words: "Yes: voice is rose. Perfect." Umber stays with Learn. THE SETTLED MAP, superseding every earlier version tonight: **sage = the words** (Transcription, the Input band); **lavender = the melody** (the `Markup` tab, the `Melody` band, the turning layer) — note lavender's idea NARROWS from "notation and voice" to just the melody, now that Voice extracts; **cobalt = information** (Guide, the Piece band); **rose = your voice** (the new Voice band, the whole calibration interface, and Insights, which is your voice explained); **umber = the book** (Learn). Five ideas, five hues, one to one, and every travelling hue travels because the band holds that idea.
- 2026-09-13, CONSEQUENCE of the Voice ruling, NOT YET RULED, flagged so nobody discovers it mid-build. The calibration pacifier is lavender today and its tokens are named in the Shane pacifier spec v11 §13: `--surround-shane` `#D8D0E0` (8 uses, "the pacifier band, on white"), `--muted-lavender` `#A89BB5` (8), `--light-lavender` `#C4BACF` (4), and `--deeper-lavender` `#8E7E9B` as the captured outline (`Pacifier.svelte:715-720`). **If Voice is rose, the pacifier belongs to Voice and recasts to rose**, which changes values a ratified spec names and which `contrast.test.ts` asserts a literal for (`--surround-shane`). Dann to rule whether the pacifier follows Voice into rose or stays lavender as an exception. The desk has NOT decided this.
- 2026-09-13, RULED BY DANN, the desk-head names. **`Transcription` becomes `Text`, on desktop and phone alike, for consistency.** His reason: the September conflict was with a section also called Text, and that section is gone (`group.text` deleted in both languages in increment 3, the Text fold's last reader, Dann having ruled the fold away 2026-09-10 late). So the name is free. Together with `Score markup` becoming `Markup` on the tab and `Melody` on the band, the head reads **Text · Markup · Insights**, then Learn and Guide. MEASURED by the desk on the live build in Dann's Chrome, 2026-09-13: Text 51.96, Texte 59.10, Markup 71.69, Insights 78.45, Words 65.19, Learn 37.48, Guide 36.15, Leçons 45.46; head available 342 px at 390 px viewport, flex gap 16, link gap 14.4. **ENGLISH NOW FITS: 202.10 for the three documents plus 88.03 for the links plus the 16 gap is 306.13 against 342, with 35.87 px spare. Learn and Guide therefore do NOT need to move to the top bar, and the desk's proposal A is retired.** FRENCH NOT ESTABLISHED: Texte and Leçons cost 15 px more than their English, and the Markup tab's French is unruled. « Partition annotée » was ratified 2026-08-19 for the OLD name; the short form « Annotation » is now free, since the band is Melody rather than Markup. At ten characters it may push French back over 342; one measurement settles it once Dann rules the word. Small note offered once: `Text` is not identical in French, which takes the e, « Texte ».
- 2026-09-19 (Dann), **FOR N.84: ADD ROBERGE, AND THE OQLF, TO THE GUIDE'S LICENCES AND ACKNOWLEDGMENTS.** The section is `GuideContent.svelte`, `id="guide-licences"`, French at `:270` (« Licences et remerciements ») and English at `:545`. **CORRECTION, and it is the desk's:** the desk reported earlier the same day that this page did not exist. It does. The desk had grepped `i18n.ts`, and **the Guide's prose is not in the strings file; it is inline in `GuideContent.svelte`, both languages.** Dann caught it.
  **WHAT TO ADD, AND WHY IT IS A NEW PARAGRAPH RATHER THAN A LINE.** The page's own promise is *"Everything Ilya ships with, acknowledged in one place"*, and its paragraphs are Scholarship, Dictionary data, Texts, Fonts, Software, Exporting. **Roberge is not a component Ilya ships; it is an authority the French is settled against**, and so is the OQLF, which governs the punctuation by Dann's ruling of 2026-08-21 and **is currently unacknowledged anywhere**. One paragraph covers both.
  **PROPOSED, English, for Dann to rule on with the rest of N.84:** **Language.** *Ilya*'s French follows the Office québécois de la langue française on spacing and punctuation, and Marc-André Roberge's *Guide des difficultés de rédaction en musique* (Université Laval) on musical terminology. **The French half is drafted when the section is.**
- 2026-09-19, **STANDING AUTHORITY FOR FRENCH MUSICAL TERMINOLOGY: Roberge's GDRM**, alongside the OQLF, which governs punctuation by Dann's ruling of 2026-08-21. **First use, the same day:** it settled *m.* against *mes.* The entry at `08-abrev.htm` gives « mes. » as the French abbreviation and states that **m.** and **mm.** are the abbreviations of the ENGLISH *measure*, with German taking **T.** So *m.* in a French string is an anglicism, not a shorter French form. `04-notes.htm` adds that prose writes *mesure* out in full and the abbreviation is for parenthetical and reference use, which is what Ilya's measure tag and witness line are. **Read the entry verbatim; a summary of that page stated the opposite.**

- 2026-09-13, RATIFIED BY DANN. **THE DESK-HEAD AND DRAWER-BAND NAMES, BOTH LANGUAGES, CLOSED.** The French mirrors the English rather than being chosen independently; Dann's clarifying words: "we need the French to correspond to the English so please use the English as template and align the French properly." TABS: `Text` / « Texte », `Markup` / « Annotation » (SINGULAR, mirroring the singular English, and the noun the ratified « Partition annotée » of 2026-08-19 is built from, so nothing is coined), `Insights` / « Aperçus ». DRAWER BAND: `Melody` / « Mélodie ». No collision in either language: the tab is Markup/Annotation, the band is Melody/Mélodie. SUPERSEDED by this line: the 2026-09-12 ruling making the band `MARKUP`, and « Annotation » as the BAND's French (it is the TAB's French now); also superseded, the intermediate French rows Text/Annotation/Aperçus, Text/Mélodie/Aperçus and Texte/Annotations/Aperçus, all of which Dann tried and set aside.
- 2026-09-13, RATIFIED BY DANN, the geometry that makes it fit. **Tab padding goes from 0.7 rem to 0.5 rem**, that is 11.2 px to 8 px on each side of each tab, returning 19.2 px across the three. MEASURED on the live build in Dann's Chrome, head available 342 px at a 390 px viewport, flex gap 16, link gap 14.4, links Learn 37.48 / Guide 36.15 / « Leçons » 45.46; tab widths at today's padding Texte 59.10, Annotation 98.20, Annotations 105.42, Aperçus 76.25, Text 51.96, Markup 71.69, Insights 78.45, Mélodie 74.60, Melody 69.69, Words 65.19, Transcription 117.05. WITHOUT the padding change the French row is 345.56 against 342, over by 3.56 (plural would be over by 10.78). WITH it: English 286.93, spare 55.07; French 326.36, spare 15.64. **NOT DONE AND OWED: nobody has looked at 0.5 rem padding on screen. It wants one render before it ships.** The desk's earlier proposal A, moving Learn and Guide to the top bar, is RETIRED: the renames plus this padding make it unnecessary.
- 2026-09-13, RULED BY DANN, three. (a) **THE PACIFIER FOLLOWS VOICE INTO ROSE.** His words: "If we like rose for voice, then the Pacifier intake for vowel formants should be rose. Done." So `--surround-shane` `#D8D0E0`, `--muted-lavender` `#A89BB5`, `--light-lavender` `#C4BACF` (which the census says paints nothing) and the captured-outline use of `--deeper-lavender` (`Pacifier.svelte:715-720`) all recast to the rose family. NOTE FOR WHOEVER BUILDS IT: the Shane pacifier spec v11 §13 names these tokens and `contrast.test.ts` asserts a literal for `--surround-shane`, so the spec and that test move with them. (b) **TRANSPOSITION IS N.94 RETURNING, NOT A NEW CARDINAL.** N.94 was numbered by Dann 2026-08-24 as "transposition interface, modelled on Newzik" and has never been built; its prior rulings 9 to 14 and the DETENTED-RULER SPEC are in `claude/e31-late-rulings-and-the-transposition-control_2026-08-07.md`, 37 days old, amendments unchecked per tether 17. Established by the desk 2026-09-13: **the ENGINE already exists and ships.** `packages/score-parser/src/transposition.ts` exports `transposePitch`, `spellPitch`, `transposeScore`, `intervalName`, `keyNameAfterTransposition` and `suggestTranspositions`, built so the watch list's range line names computed keys rather than guesses (Dann's ruling 2026-07-20), and it is wired at `watchlist.ts:476`. What is missing is only the control that transposes the score for the singer. N.94 now lives as a station inside the `Melody` band, sibling to Corrections. (c) **THE SERIF WIDTH TABLE IS N.129**, assigned by Dann 2026-09-13. Number confirmed free before assigning: nothing in `docs/` used it. Full finding in the 2026-09-12 line above; it needs promoting from INBOX into STATE.md.
- 2026-09-13, TASK FOR THE NEXT SESSION, recorded so it cannot be forgotten. `STATE.md` is 901 lines against a 600 tripwire. A `FIRST, BEFORE ANY OTHER WORK` block now sits at the top of THE ONE THING naming the six blocks that move to `../sessions/LOG.md`, the two sections to assess row by row, and the four things that must NOT move because they are the live handover. It instructs the session to delete that block once done. The 2026-09-12 session left the surgery undone on purpose: every other edit that night was additive and safe, and cutting two hundred lines out of the handover at 03:00 was the one operation that could have damaged it.
- 2026-09-13, DANN'S ASK, QUEUED FOR IMMEDIATELY AFTER THE COLOUR STORY CLOSES. **A sequential, linear checklist he can use as a roadmap to a satisfactory release.** His words and his frame, recorded because they change how it must be built: *"There is already a functioning version of Ilya with transcription only, available out in the world right now. What we are working on is my dream release, the functionality I want it to have. No one is pressuring me to release this and there is no externally-imposed deadline. Nevertheless, I am feeling cognitive fatigue after nine months of development."* He asks the desk to read all open issues, all standing cardinal numbers, anything uncommitted in `INBOX.md`, and anything the desk infers is necessary from related pieces; he EXPRESSLY asks for inference here, which is tether 21's exception, so every inferred item is marked DESK INFERENCE on its face. Purpose in his words: *"help me remain motivated as we check things off, and show incremental progress toward a satisfactory release."* **THE DESK'S CRITIQUE, given 2026-09-13 and recorded so the work starts from it: the checklist is downstream of THE RELEASE CUT** already sitting at the top of `STATE.md` §RULINGS DANN OWES. Built over the live queue it lengthens as he ticks it, which is the opposite of what he is asking for: 2026-09-07 to 2026-09-13, fifteen numbers arrived and nine units closed. So the cut comes first and the list is built only over the frozen set. Second: the satisfying unit in this project is a deploy and a walk (schema rule 4), not a tick, so the roadmap groups into walkable milestones rather than a flat list of numbers. Third: the scour is bounded to what a release requires, or it manufactures the very backlog he is fatigued by.
- 2026-09-13, RULED BY DANN, THE RELEASE SENTENCE. His words, written by him after the desk proposed a weaker one: *"Ilya takes your text, your score, and your voice, and shows you on the page three helpful supports: a formatted IPA transcription, where the piece will sit well for you and where it will not, and offers insights on how (or whether) you might decide to move forward with the piece."* **This is the gate for the release cut: an item is IN only if it serves this sentence.** DESK OBSERVATION, not his: the sentence's three supports map one to one onto the three documents ratified the same day, `Text` / `Markup` / `Insights`, so the cut runs per document rather than per item. The three buckets the desk proposed and he has not yet ruled on: IN (serves the sentence, finished, walked), FLAGGED (built, delightful, unfinished, ships behind `PUBLIC_INCLUDE_SHANE` with the switch off, `apps/web/src/lib/wall.ts:7`), LATER (not started). **The desk's standing promise, to be held to it: nothing goes to LATER for being hard or risky, only for not being started.**
- 2026-09-13, RULED BY DANN, THE RELEASE SENTENCE, FINAL WORDING. He chose option A after asking for edits to his own draft; the desk's note was that his three supports were three different grammatical shapes, so the list broke after the colon. **THE SENTENCE: *"Ilya takes your text, your score, and your voice, and gives you three supports on the page: a formatted IPA transcription, a reading of where the piece will sit well for you and where it will not, and insights to help you decide whether to sing it."*** This supersedes the draft in the line above it; the draft's substance is unchanged and only its grammar moved. "whether" is deliberate and matches the standing rule that Ilya forecasts and never declares. **This is the gate for the release cut.**
- 2026-09-13, ESTABLISHED BY THE DESK, READ THIS SESSION, and it answers a question Dann asked. **THE CALIBRATION WHEEL'S FLOOR POLYGON IS NOT THE VOWEL QUADRILATERAL.** `Pacifier.svelte:215-221`: it is an achievement overlay, a PENTAGON joining the five anchors {[i], [e], [ɛ], [ɑ], [u]}, drawn by angle around the shape's centre so it cannot self-intersect, which lights in behind the vowel nodes once all five are sampled. **The quadrilateral is the wheel's underlying geometry, which the code itself calls "the locked Jones geometry" (`:212-213`).** The comment states the five-anchor floor "targets Grayson's schema, not phonetic vowel-space completeness. JUDGEMENT", and records that a citation to Fox and Jacewicz 2017 was removed 2026-07-17 as speech-dialect VSA work that critiques corner-vowel sets rather than endorsing a five-vowel minimum. **TWO THINGS OWED, both named in the same comment and neither done:** contrast for this overlay is NOT a locked obligation in `contrast.ts`, and wizard spec v1 §5 flags the rendering open, "confirm it reads as intended at the port". Nobody has confirmed it. **DESK OBSERVATION, not ruled:** the floor polygon reports a STATE ("you have enough to proceed"), not an identity, so under Dann's grammar it may not want a document hue at all; today it is `--sage` (`Pacifier.svelte:768`), which the pacifier-to-rose recast of 2026-09-13 does not mention.
- 2026-09-13, RULED BY DANN, THE RELEASE CUT'S THREE BUCKETS. His words: *"Yes to the three buckets you propose, this is an excellent model."* **IN**: it has to be there on day one, and it is finished. **FLAGGED**: real, half-built, not ready; it ships inside the code with its switch off (`apps/web/src/lib/wall.ts:7`, `PUBLIC_INCLUDE_SHANE`), so nobody sees it until it is done, and nothing is deleted. **LATER**: nobody has started it; it waits for the release after this one. **THE STANDING PROMISE, which is the reason he accepted the model: nothing goes into LATER for being hard or ambitious, only for not being started. Hard and half-built goes into FLAGGED, and FLAGGED is the first work after launch, not the last.** The gate for IN is the release sentence he ruled the same day. The inventory to sort is `docs/sessions/inventory-release_r1_2026-09-13.md`, 114 open items. **Not yet sorted.**
- 2026-09-13, ESTABLISHED BY THE DESK, MEASURED, AND IT IS BIGGER THAN IT LOOKED. **FRENCH PARITY. 122 of `i18n.ts`'s 622 single-line entries carry French identical to their English**, parsed this session with zero entries unparsed. Some of the 122 are identical on purpose, because the word IS the French word (`Notation`, `Reconstitution`, `Provenance`, `Source`, `Transcription`, `Guide`, `Passaggio`, `Actions`, `Pause`, `Hz`, `m.`, `Arr.`, and the IPA and glyph strings such as `ʌ → ə` and `ё ↔ е`). The rest are untranslated. **THE HEADLINE: INSIGHTS IS ENTIRELY UNTRANSLATED.** `i18n.ts:1417-1475`, about 58 consecutive entries, every one English in both columns: the identity line, the fit table and all its column heads, every flag, every verdict, every finding, the footnotes, the method lines and the silence lines. **N.127 increment 1 shipped 2026-09-13 with no French at all**, so a singer in French mode meets Ilya's third document speaking English throughout. Nothing here is coined and no French is written: this is a count, not a translation. **Bears directly on the release cut**: the sentence Dann ruled names Insights as one of three supports, and reading correctly in both languages is one of the three tests the desk bounded the cut's inferred items to.
- 2026-09-14. Code, on the N.138 increment 2/3 walk: the selection ring can land UNDER the system's opaque ground, on the page and in the loupe. `VoiceProfilePane.svelte:533-540` walks past leading full-width rects only, and the loupe's own `data-held-measure` mark (`Loupe.svelte`, `insertBefore(mark, sysEl.firstChild)`) now sits first, so the walk stops before the ground. **Pre-existing, outside N.138, NOT ESTABLISHED as the cause of the missing box on Sunless m. 18 because the pane was hidden.** Details: `memo-n138-loupe-meter_r1_2026-09-14.md` §7.7.
- 2026-09-15. CORRECTION to the entry above of 2026-09-07, made after Code searched for it: **the paste it says was "sent to a second Code thread" does not exist.** It is not in `INBOX.md`, not in `docs/sessions/`, and no commit has touched `stripBackingRect` since. Nothing ran. **The ruling it carried is now built anyway**, as N.133 in `eb918ed`, and the printed page is white because `app.css` overrides `--paper-cream` under print and the renderer no longer paints a rectangle the token could not reach. **Do not go looking for that paste.**
- 2026-09-15. TRANSCRIBED, and the item above of 2026-08-27 is now closed: the squircle-as-visual-germ ruling is in `PRODUCT.md` §The squircle, together with Dann's ruling of 2026-09-15 that the squircle's purpose is arresting focus and that its negative space is the point. **Nothing further is owed on that inbox line.**
- 2026-09-15, Dann, mid-N.141: **a second job for the squircle on the page, alerting the singer to acoustic events**, his words *"conflicts or exceedances between the sung pitch and the prescribed vowel's pitch-of-turning"*. He put it as a distinction: the loupe's squircle is focus, the page's would be alert, *"even though we use this familiar shape for both purposes"*. **NOT a clarification of N.141 and it does not amend it**; N.141's page mark is SELECTION and exists today (`VoiceProfilePane.svelte:502`). The design question it raises: one shape, two meanings on one surface, told apart only by context. Today the acoustic information is carried by the turning layer's lavender noteheads instead (`staff-renderer.ts:2440-2454`, `data-analysis="turning-*"`). **Not numbered. Dann rules it in or lets it sit.**
- 2026-09-15. **T05 HAS DRAWN NO IPA ROW IN THE PANE THREE TIMES, in three separate Code runs on N.141**, each time attributed to the load rather than investigated: once to an empty poem box, once to the dictionary being throttled while the browser pane was hidden, once unexplained. **Each time it cost a NOT ESTABLISHED**: T05's vertical grammar, then its IPA clearance, then the new IPA-collision check. **Three times is a pattern, not a coincidence.** Kabalevsky T05 is one of Dann's three scores and it carries 146 Cyrillic syllables with 0 IPA in the pane. **Either the fixture load is wrong in a repeatable way, or the IPA row genuinely does not draw for this score.** Nobody has looked. Not numbered; raised for Dann.

- 2026-09-15, close. **N.126 confirmed by Dann on the page:** *"4 [28] reads
  exactly as it should."* The last open question on it is closed.
- 2026-09-15, close. **The desk let a correct finding reach Dann as an excuse.**
  N.125's record said the constant-width arcs are slurs and ties already taper.
  True, and re-verified. But it arrived as a reason the arcs looked as they did
  rather than as the location of the fix, so he read it as a refusal and had to
  push twice. **The finding was the answer to "which block", never to "should
  this taper".** Say which question a finding answers.

- 2026-09-16, Dann RULED (build, unplaced): Ilya operates on a complete set of Grayson's ten sung vowels. Bozeman's values are surrogate data until the singer offers a reliable sample: no vowel is ever blank or without pitch-of-turning information. Any gap is filled from them, flagged, placed on a continuum between Bozeman's charts by the singer's samples (Dann 2026-09-16: no single chart is picked); Dann 2026-09-16: values are derived mathematically around whatever samples exist, forming a hybrid of sung, derived, and surrogate values. Ilya's derivation ratios (derivations.ts:19-35) reproduce Mitton 2020 Table 5.3 within 2 Hz, i.e. they are fitted to one voice. Amends Kimi 2026-07-11 (self-declaration the only routing input, plausibility.ts:34-38): Dann 2026-09-16, a declared voice type plays NO part in choosing values, because fach is bound up with identity and Ilya makes no pronouncements on a singer's identity; the matched chart is internal and its voice-type name is never shown. Open: the surrogate when too few vowels are sampled to match (the code's all-voices union bands are one candidate); what counts as a reliable sample; one number from a band; minimum samples to match; the four vowels Bozeman does not chart; fR2 (bands are fR1 only, plausibility.ts:48).

- 2026-09-16, Dann ASKED, on the N.143 walk: should dropping a score be as instant as dropping text, with no "Continue to analysis" pause? Desk answered with three options (instant for all; instant except picture and PDF reads; keep the pause) and recommended the middle one. His ruling is not yet given. **RULED 2026-09-16: option 2; numbered N.145.**
- 2026-09-16, seen on the N.143 walk: the poem receipt reads "1 lines". `intake.lines` (`i18n.ts:620`) has no singular, and its French slot is English ("%s lines"). A poem filled from a score arrives as one line because the score carries no line breaks.
- 2026-09-16, Dann, UNACCEPTABLE: in the loupe a tap on a note both navigates and places the armed syllable (`handleLoupePick`, `+page.svelte:661-664`: `setCursor` then `placeArmedSyllable`), so a singer moving around the loupe reassigns syllables by accident. He wants navigation and placement separated. Desk offered four designs, recommended: a tap only selects, and placing is its own labelled control beside the armed syllable (plus Enter on a desk), with the selection advancing after each placement. The page-tap rule of 2026-08-26/27 put placement inside the loupe; this would reverse that half. His ruling is not yet given.
- 2026-09-16, seen on the N.142 walk (`c868540`, T05, incognito): **Start placement over breaks the score's own melismas.** m. 22: the slur over the first two eighths holds « ёк »; after the press « сту » sits on the second slurred note, every later syllable is one note early (« О » and « гонь » land before the four-bar rest), and an extender line runs under « сту-дё ». Cause read 2026-09-16: `handleStartPlacementOver` seats with `firstPass` (`pairings.ts:276-288`, slot i on target i). The fix already exists as `seatFilledPoem` (`+page.svelte`, N.134), which seats from the score's own cells via `seatScoreWords` (`score-seat.ts:78-121`). DESK READING for the sort: meets the freeze rule's exception (wrong placements, not undoable). Dann asked how hard; desk answered small, recommended a Start-over branch that reuses `seatFilledPoem` when the score carries words, plus a `pushUndo`.
- 2026-09-16, seen on the N.144 walk in French mode: the drawer's SCORE receipt draws its label « PARTITION » run into the file name (« PARTITIONKabalevsky… »), no gap. Also English in French mode on the same screen: PIECE, INPUT, SCORE MARKUP, POEM, Clear, Replace, "Drop the other kind here…", "words", Notation, "Transcribe and fit" (N.131 and N.132 territory).
- 2026-09-16, Dann, during the sort: *"Notation toggles should also be instantaneous on Transcription and Markup. Nothing happens when I toggle Open syllables right now and I should see activity on the page immediately."* DESK: widens N.136 (Markup only) to both documents and to every notation toggle; N.136 and N.119 are IN, so this rides with them.
- 2026-09-17, Dann: Ilya is his legacy and should help others build for other languages. After the release: map which parts of the tree are Russian-specific and which carry over, then write a guide for building a sibling (`PRODUCT.md`, "Why Ilya exists").

- 2026-09-21, Dann ASKED, on ruling N.132 row 3 in: re-develop GUIDE. His words: *"I bnet there are a ziullionplaces where we will need to update to these new names"*. The Guide prose carries the old tab names, and Learn probably does too. Note only, not numbered.

- 2026-09-21, Dann, on scoping the N.154 sweep to `i18n.ts` and leaving Guide and Learn out: *"I agree. I suspect guide and Learn will undergo radical rewrites, so this is an appropriate plan."* **So a name-sweep of the Guide and Learn prose may never be worth running**; their strings would be rewritten wholesale rather than patched. Bears on the earlier note the same evening.
