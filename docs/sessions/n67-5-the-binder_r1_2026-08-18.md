# N.67 STEP 5, THE REMAINDER — export all, multi-song import, the collision rules

**Written 2026-08-18, against `924f687`, working tree clean at the start.**
**Shipped as `9892887`, 10 files, 1,834 insertions, 111 deletions, all five
gates at baseline.**
**Every claim below carries a `path:line`, a run I made, or the words "not
established." There is no fourth form.**

---

## 0. Two corrections to the brief, before anything else

**The brief was written against `ed8318e`. HEAD is `924f687`.** Two commits had
landed since (`6ba87e1`, `924f687`, both GUI). Nothing the brief relies on
moved, but every line number it cites is off by a few, and the tree won each
time. The real ones are in §5 below.

**The gate 4 baseline in the brief's world is 555. In `ilya-ship.sh:79` it is
590,** and 590 is what the tree ran at before I touched it.
`docs/memory/ENVIRONMENT.md` still says 555 and is stale; §6 has the paste.

**Everything else in the brief checked out.** `readBinder`'s plural loop
(`binder.ts:190-225` as cited, now `:213-249`), the single-song assumption at
one line (`read.songs[0]`, cited `:1017`, actually `:1018`), `uniqueName`
(`songs.ts:65`), `newId` (`library/index.ts:61`), and the reason `library.load`
cannot detect a collision (`library/index.ts:164-166`).

---

## 1. What ships

| file | what happened |
|---|---|
| `apps/web/src/lib/library/exchange.ts` | **NEW.** 313 lines. Every rule in this step. |
| `apps/web/src/lib/library/exchange.test.ts` | **NEW.** 594 lines, 34 tests. |
| `apps/web/src/lib/library/binder.ts` | `buildBinder` takes an array. `BinderSong` is now one type for the writer and the reader. |
| `apps/web/src/lib/library/binder.test.ts` | Moved to the array form; four new two-song tests. |
| `apps/web/src/lib/i18n.ts` | `import.title` and `import.body` retired; eight approved keys added. |
| `apps/web/src/routes/+page.svelte` | Dialog, anchor, file input. The stale comment repaired. |
| `apps/web/src/lib/components/Drawer/RootPanel.svelte` | The twin's third control. |

**Gates, before and after, all five run on this machine:**

| gate | before | after |
|---|---|---|
| phonology | 216 | 216 |
| dictionary | 235 | 235 |
| web-check | 0 errors, 7 warnings, 4 files | **unchanged** |
| web-test | **590** | **628** |
| score-parser | 444 passed, 5 skipped | unchanged |

**Gate 4 moves 590 to 628 and needs your permission.** `ilya-ship.sh:79` reads,
verbatim:

```
gate 4 web-test     "590 passed (590)"                          pnpm -C "$REPO" --filter @ilya/web test
```

**Three files are untracked and the ship script refuses on untracked files
anywhere in the repository (`ilya-ship.sh:45-50`):** `exchange.ts`,
`exchange.test.ts`, and this memo. They need `git add` before you ship.

---

## 2. The walk

Run against the dev server in the in-app Chromium at 1400×900. **Expectation
stated before each measurement, as asked.** Two refuted expectations and one
outright defect are on the record below; they are the most useful part of this
section.

**How the two songs were made, stated plainly because it bears on what the walk
proves.** I wrote two complete song records and their score bytes straight into
`ilya-library` through the console, then reloaded so the app read them through
its own boot path. The bytes are real fixture files
(`tools/e16-harness/b1-ledger-lines/b1-ledger-lines.musicxml`, 4,849 B, and
`close-fixture/close-fixture.musicxml`, 6,873 B). Song A carried 7 placements,
song B carried 4. **The app booting from them, and drawing their titles,
composers, poems and names, is what establishes they were well-formed records.**
Everything after that ran through the real UI: real button clicks, and real
`change` events carrying real `File` objects into the real file input.

**Two instruments, named.** `URL.createObjectURL` was wrapped to record the
`Blob` the export hands the anchor, and `HTMLAnchorElement.click` was stubbed so
a download sheet would not block the run. **The export code itself ran exactly as
shipped**; the instrument only watched what came out of it.

### Positive control, before the collision check existed

**Expectation: on the old path, a re-import of a song already in the library
overwrites it and reports success.**

Run against the real IndexedDB driver, replaying `commitImport`'s single call
verbatim:

```
  save reported: {"ok":true}
  poem before:  THE WORK I DID
  poem after:   THE FILE I OPENED
  placements before: 2
  placements after:  0
```

**MET. The brief's §2 is correct.** A poem and both placements destroyed, and
the only thing the app was told was `ok: true`. This is the defect the step
closes.

**What this control is not:** it is a node control at the layer where the defect
lived, not a browser one. A browser control would have needed the old build, and
reverting the tree to get it was not worth the risk.

### 1. Export all

**Expectation: three controls in the binder row where there were two, and one
file of roughly 5 to 9 KB.**

Three controls, in both rows: `Export this song`, `Import a song`,
`Export all songs`, the third sitting in a grid column measured at 237 px that
had stood empty since the row was built (`1fr 1fr 2fr`, `+page.svelte:2204-2209` and
`RootPanel.svelte:487-493`). **MET.**

One file. **4,042 bytes**, `application/zip`, offered as
`Ilya, August 18, 2026.ilya`. **The size expectation was REFUTED** — smaller
than my floor, because both sources are plain XML and deflate hard (11,722 B of
score went in). I had reasoned about the deflated total too conservatively.

Its members, read out of the central directory:

```
manifest.json
songs/walk-song-b/song.json
songs/walk-song-b/source/close-fixture.musicxml
songs/walk-song-a/song.json
songs/walk-song-a/source/b1-ledger-lines.musicxml
```

Grouped by song, manifest first, in the order the list draws them.

### 2. Clear and import

**Expectation: both songs return with names, 7 and 4 placements, sources of
4,849 and 6,873 bytes, a notice reading "2 songs were added", no dialog.**

Library emptied, page reloaded, the 4,042-byte file fed to the input. **MET on
every count.** Both records back with their titles, poems and names; placements
7 and 4; sources 4,849 and 6,873 bytes each keyed to its own song. No dialog.

**But the notice was invisible.** With the drawer on Transcription, where the
Import control was pressed, `document.querySelectorAll('.shane-storage-notice')`
returned nothing. Switching to Fit showed `2 songs were added.` **The weakness
the brief named at §5 bit exactly as predicted, on the first import of the
walk.** Not moved, per instruction. §4 carries it.

### 3. The collision dialog

**Expectation: one dialog per song, two ISO dates, Keep mine last in the DOM and
focused, and the dialog centred rather than at (0,0).**

**MET.**

```
title    You already have this song.
body     ... Yours was last changed 2026-08-18. The one in this file was
         last changed 2026-08-12. Ilya cannot undo taking the one in this file.
DOM      ["Take the one in this file", "Keep both", "Keep mine"]
focused  Keep mine
rect     x 444, y 357, w 512   in a 1400×900 viewport
:modal   true
```

**(444, 357) and not (0, 0).** `app.css:89-93`'s reset still overrides the user
agent's `dialog { margin: auto }`, and `.replace-dialog`'s own `margin: auto`
(`+page.svelte:2229`) still repairs it. Read, not inferred, as asked.

### THE DEFECT THIS WALK FOUND, and it would have shipped

**On the first attempt at step 4, the second dialog never opened.** Answering
the first collision left `pendingConfirm` set, the dialog shut, and the import
hung forever on a promise nothing would ever resolve.

I had written `askCollision` to resolve **from the dialog's `close` event**,
reasoning that a press resolving first would let the next `showModal()` run
before the current dialog had finished closing. Instrumenting
`HTMLDialogElement.prototype` showed why that failed:

```
9407  showModal OK
10069 --- clicking Keep mine ---
10069 close() called
11069 final: open=false hasContent=true
```

**`close()` was called and no `close` event ever fired.** Confirmed on a bare
`<dialog>` created in the console with no framework near it:

```
open=true / after close(), open=false / close event fired: NEVER
```

**Fixed by resolving from the press instead**, with a guard for the ordering
problem that drove the original design: `onclose` now returns early when the
dialog is already open again, so a late close event cannot blank the question on
screen nor answer it for the singer (`+page.svelte:1697-1705`).
`oncancel` also settles, so whichever of the two arrives first wins and the rest
are no-ops. After the fix, the second dialog opens, focused, at (444, 357).

**This is the strongest argument in the memo for the walk being a gate.** All
five gates passed with the hang live. Runes are inert under vitest, so nothing
in `exchange.test.ts` could reach it — the module was correct; the page was not.

### 4. Keep mine, then Take on a song you are not in

**Expectation: Keep mine writes nothing; Take updates that song and does not
move me; no reload.**

**MET on every count.**

| | before | after |
|---|---|---|
| song B, answered **Keep mine** | `updatedAt 23:32:27.918Z` | `23:32:27.918Z`, unmoved |
| song A, answered **Take** | `23:32:27.919Z` | `23:36:19.637Z`, updated |
| `ilya:activeSongId` | `e53d05f1…` | `e53d05f1…`, unmoved |
| window canary | alive | **alive** — no reload |

No notice, because nothing was added. That is `importNoticeKey`'s rule and it is
deliberate; §4 records the doubt about it.

### 5. Keep both

**Expectation: a third song, name numbered, its score and placements intact and
independent of the original.**

**MET.**

```
1ed8e74c-eae0…  Mussorgsky, В четырёх стенах (2)   4 placements   source 6,873 B → songId 1ed8e74c-eae0…
walk-song-a     Kabalevsky, Сонет 90               7 placements   source 4,849 B → songId walk-song-a
walk-song-b     Mussorgsky, В четырёх стенах       4 placements   source 6,873 B → songId walk-song-b
```

**The source's own `songId` moved with the record**, which is the trap the brief
flagged: the sources store is keyed on `songId` (`driver.ts`, the idb driver's
`store(SOURCES_STORE).put(source)`), so a copy whose bytes still named the old
song would have drawn its neighbour's music. Notice: `One song was added.`
No reload.

**One cosmetic consequence, observed:** re-importing a binder of the copy
produced `Mussorgsky, В четырёх стенах (2) (2)`. `uniqueName` numbers the base
it is given, and the base genuinely was `… (2)`. Correct per design §2.3, and it
looks odd. Not changed; naming it in case you want it changed.

### 6. Take, on the song you are in

**Expectation: this is the only path that reloads.**

Opened song A, exported it alone (**2,136 bytes**, offered as
`Kabalevsky, Сонет 90.ilya` — a one-song binder wears the song's name), then
imported it back while still in it.

The dialog opened. **That is the named consequence the brief accepted at §1**,
and it is the one moment the question is worth asking. Answering **Take**:

```
canary gone → RELOADED
activeSongId  walk-song-a   (unmoved)
title         Сонет 90
poem          Я вас любил любовь еще быть мо…
```

**MET.** The only reload in the entire walk.

### 7. The retirement is observable

**Expectation: with a song full of work open, importing a binder of a song you
do not have shows no warning at all and leaves the open song untouched.**

Song A open, carrying a poem, 7 placements and a 4,849-byte score. Imported a
binder of a song the library did not hold, watching the dialog's `open`
attribute with a `MutationObserver` and a 30 ms poll for the whole run.

```
dialogEverOpened  false
reloaded          false
activeSongId      walk-song-a   (unmoved)
song count        2 → 3
open song name/title/poem/placements/updatedAt   IDENTICAL before and after
open song source  4,849 B → 4,849 B
```

**MET.** `import.title` and `import.body` are gone from the tree and from the
screen.

### 8. The French, and the twin

Not a numbered step; checked because the copy is approved copy.

```
Vous avez déjà ce chant.
Le chant de ce fichier a la même identité qu’un chant que vous avez déjà.
Le vôtre a été modifié pour la dernière fois le 2026-08-18. Celui de ce
fichier a été modifié pour la dernière fois le 2026-08-18. Ilya ne peut
pas annuler le remplacement.
["Prendre celui de ce fichier", "Conserver les deux", "Conserver le mien"]
focused: Conserver le mien
row:     Exporter ce chant | Importer un chant | Exporter tous les chants
notice:  Un chant a été ajouté.
```

Ships as approved, both languages, no `[MISSING:` anywhere on the page. The Fit
row and the Transcription row both carry the third control, in both languages.

**Regression check, unasked for but cheap:** the delete dialog still opens,
still reads `["Delete this song", "Export this song first", "Keep this song"]`,
and still deletes. My `onclose` change did not disturb it.

---

## 3. Measurements

| thing | figure |
|---|---|
| binder of 2 songs, 11,722 B of score between them | **4,042 bytes** |
| binder of 1 song, 4,849 B of score | **2,136 bytes** |
| binder of 1 song, 6,873 B of score | **2,372 bytes** |
| import wall-clock, 2,372 B binder, change event to record in the vault | **469 ms** |
| `+page.svelte` before | 2,857 lines, 105,544 bytes |
| `+page.svelte` after | **2,938 lines, 109,542 bytes** |

**The 469 ms is an upper bound, not a clean figure.** It is polled at 10 ms
granularity over a held connection. My first attempt read 909 ms because the
poll reopened the database every 5 ms and contended with the write; a third
attempt using `requestAnimationFrame` never returned at all, because rAF does
not fire while the browser pane is hidden. **Both of those are the instrument,
not the app**, and I am reporting the figure I trust with its granularity
attached rather than the prettiest of the three.

**Weight, against design §6's estimate.** The design carries 9 to 18 MB for a
hundred songs. **I did not measure that and cannot**: my two songs total 11.7 KB
of score against the design's 90 to 175 KB per realistic song. Scaling my figure
would be arithmetic dressed as a measurement. §4 records it.

### `+page.svelte` grew, and here is the reason

**+81 lines and +3,998 bytes. I did not achieve a net shrink and I am not going
to dress that up.**

The step 5 block itself is 159 lines: **87 code, 63 comment, 9 blank**, against
117 before. The rest is a third control in the Fit row, a second notice, the
`oncancel`/guarded `onclose` pair, and the repaired comment.

What I did do:

- **Every rule this step invents is in `exchange.ts`,** 313 lines of it, under
  34 tests. None of it existed anywhere before. Written inline, the collision
  loop, the export gathering, the naming, and the two message rules would all
  have landed in the page.
- **Two decisions that were already in the page moved out** and gained their
  first test coverage: the five-conditions-to-three-sentences mapping is now
  `binderFailureKey`, and the notice choice is `importNoticeKey`. That is what
  took the file from 2,942 back to 2,938.
- The page now holds the dialog, the anchor, and the file input, as instructed,
  plus the promise bridge between the dialog and the module — which is page
  work, because the dialog is the page's.

**Roughly two fifths of the new block is comment**, and a third of that is the
two findings in §2 that cost real time. I would rather leave them there than win
40 lines.

---

## 4. What I could not establish

**Escape.** The comment claims Escape resolves to the safe answer, and I could
not test it. A real `Escape` key press delivered to the pane left the dialog
open. **I cannot tell you whether that is the app or the harness**, and I will
not guess: this same pane provably fires no `close` event for a bare `<dialog>`,
so it is not an instrument that can falsify a dialog-dismissal path. Nothing was
written and no reload happened while the dialog sat there, so the failure mode,
if it is one, is safe. **The `oncancel` handler I added is written but
unexercised.**

**Whether `close` events fire in a real singer's browser.** The finding above is
about the in-app Chromium browser pane, on a dev server. If it holds in Safari
and desktop Chrome, then `pendingConfirm` and `pendingArrival` have never been
cleared on close since step 4a — a pre-existing condition, not mine, and larger
than this step. **My code no longer depends on the event either way**, which is
why I did not chase it. Worth chasing separately.

**Whether "nothing was added" should say nothing.** `importNoticeKey` returns
null when a run only replaced or only skipped, so answering **Take** on a song
you are not in produces no sentence at all. My reasoning is that the song moves
to the top of the list, which is visible. **The approved copy has no string for
"replaced", so the alternative was to coin one, which was not mine to do.** If
that reads as silence to you, it is a copy decision and it is yours.

**The right sentence for a partial write failure.** A run where two songs land
and one refuses sets `binderError` to `songs.err.write`, which ends "Nothing has
changed" — and something did. The old code was worse here (it said the *file*
was damaged), so this is an improvement on a defect rather than a new one, but
it is not right. **No approved string fits, so I did not invent one.**

**The weight of a large binder.** Not measured. See §3.

**Safari, and every number in this memo.** Chromium only, as every number this
project holds has been.

**Whether an import notice in the Fit drawer is seen at all.** Established that
it is NOT, when the singer is on Transcription — §2 step 2. Not solved, per
instruction. `STATE.md` marks it unsettleable alone and I agree.

**The two known open defects (F7, F8)** did not make any walk step ambiguous. I
did not touch them.

---

## 5. Citations, checked

Every file below was opened this session before it was cited. The 4b memo cited
two lines that turned out to be a tab handler and a metadata function; these are
the line numbers **after** this step's edits, and I re-read them to write this.

| claim | where |
|---|---|
| the reader has always been plural | `binder.ts:213-249` |
| a newer schema is refused whole-file | `binder.ts:209` |
| an empty binder is refused | `binder.ts:210` |
| the source carries the song id too | `binder.ts:236` |
| `binderFileName` | `binder.ts:71` |
| the writer and reader share one song type | `binder.ts:109` |
| every rule of this step | `exchange.ts` |
| `uniqueName`, design §2.3's numbering | `songs.ts:65` |
| `listSongs`, which carries `updatedAt` | `songs.ts:155` |
| a load of an absent id is an empty record, not an error | `library/index.ts:164-166` |
| `newId` | `library/index.ts:61` |
| `null` deletes the source, `undefined` leaves it | `driver.ts`, memory driver `save`, and the idb driver's `writeAcross` |
| the dialog's `margin: auto` repair | `+page.svelte:2229` |
| the reset that made it necessary | `app.css:89-93` |
| the twin binder row | `RootPanel.svelte:238-247` |
| the retired keys' replacements | `i18n.ts:632-640` |

**One thing the brief could not establish, now established:** `binder.test.ts`
did **not** cover a two-song binder read. Every round-trip test built a
one-entry manifest, so the plural loop at `binder.ts:213` had never once run
twice in a test. It does now, four times.

**One latent defect found while reading, and fixed in passing.** `commitImport`
passed `incoming.source ?? undefined` to `library.save`. `undefined` leaves the
stored bytes alone and `null` deletes them, so importing a scoreless song over a
scored one would have left the old score attached to the new record — the
chimera N.67 step 4a exists to prevent. The new path passes the `null` through,
and `exchange.test.ts` holds a test that fails if it stops doing so.

**Hard spaces: 49 occurrences in `i18n.ts` before, 49 after.** The brief said 37;
the file says 49 counting both the literal character and the ` ` escape.
Either way the count did not move, which is what mattered: none of the eight new
strings carries a colon, a question mark, or an exclamation mark.

---

## 6. For `docs/memory/ENVIRONMENT.md`, ready to paste

```markdown
## `<dialog>`'s `close` EVENT DOES NOT FIRE IN THE IN-APP BROWSER PANE. 2026-08-18

**`close()` shuts the dialog and fires NO `close` event** in the Chromium the
Claude Code browser pane drives. Confirmed on a bare `<dialog>` built in the
console with no framework near it: `open` goes true, `close()` sets it false,
and a listener added with `addEventListener('close', …)` never runs, at 300 ms
and again at 900 ms.

**Cost, N.67 step 5:** a collision dialog written to resolve its answer from the
close event hung the whole import on the first colliding song. Every one of the
five gates passed with the hang live, because runes are inert under vitest and
the module underneath was correct. **The browser found it and nothing else
could.**

- **Never make a dialog's answer depend on the `close` event.** Resolve from the
  press, and guard `onclose` with `if (dialogEl?.open) return` so a late event
  cannot blank or answer the next dialog in a sequence.
- **This also means `onclose` cleanup may never run here.** `+page.svelte`
  clears `pendingConfirm` and `pendingArrival` there. Whether that holds in
  Safari and desktop Chrome is NOT ESTABLISHED.
- **A real Escape key press did not close a modal in this pane either.** Whether
  Escape works for a singer is therefore not testable from here.

### Driving the app from the pane, the two instruments that work

- **A file input takes a real file** via `DataTransfer`: build a `File`, assign
  `input.files = dt.files`, dispatch `new Event('change', {bubbles: true})`. The
  real handler runs.
- **A download is readable** by wrapping `URL.createObjectURL` to keep the Blob
  and stubbing `HTMLAnchorElement.prototype.click` so no download sheet blocks
  the run. The export code itself is untouched.
- **`requestAnimationFrame` never fires while the pane is hidden.** A poll built
  on it hangs until the tool times out. Use `setTimeout`.
- **Vite refuses `/@fs/` outside its root** (403), so repository fixtures under
  `tools/` cannot be fetched from the page. A three-line `http.server` with
  `Access-Control-Allow-Origin: *` on another port is the way in.
- **The language toggle is `<span role="button">`, not `<button>`.** A
  `querySelectorAll('button')` search for it finds nothing and `?.click()` fails
  silently. Use `.lang-option`.
```

And the stale line, which should be corrected wherever the gate table sits:

```markdown
| web-test | **628** |
```

**`ENVIRONMENT.md`'s gate table says web-test 555. `ilya-ship.sh:79` said 590
before this step and the tree ran at 590.** The table has been stale by two
moves; 628 is the number after this step.
