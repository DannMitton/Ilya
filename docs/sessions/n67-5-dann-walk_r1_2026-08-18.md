# N.67 step 5: Dann's walk on the deploy, 2026-08-18 evening

**Walked by Dann in his own Chrome on deploy `ilya-eaxv09qx3` (commit
`a8a979b`, which contains `9892887`), curated live by Fable, one instruction at
a time, every expectation stated before its measurement. This walk is what
moves step 5 from WRITTEN to DONE.**

## The steps, expectation against observation

1. **Fresh origin.** Expected empty drawer, one Untitled song, dictionary
   loading. **Met**, plus the install prompt appeared (not predicted, not part
   of the walk).
2. **Paste and transcribe `Я вас любил:...`** Expected stacks, and the F7
   one-letter auto-name `Я`. **REFUTED, favourably: the song auto-named
   `Я вас любил: любовь`.** F7 needs re-verification rather than a fix; either
   tonight's ship fixed it in passing or it was misdiagnosed.
3. **New song, `Не пой, красавица...`** Expected a second auto-named song.
   **Met**, and **Export all songs appeared only at two songs**, confirming the
   conditional live.
4. **Export all.** Expected one binder under Code's 4,042-byte two-song figure
   (ours had no scores). **Met: `Ilya, August 18, 2026.ilya`, 1,454 bytes**,
   clicked twice by accident, both byte-identical.
5. **Clear site data, reload.** Expected factory-fresh. **Met**: both songs
   gone, one Untitled, Export all correctly hidden again.
6. **Import the binder.** Expected both songs back by name, list of three, no
   warning dialog (the retirement observable), and no success notice (the
   named Fit-drawer-only weakness). **All met.**
7. **Open a returned song.** Expected the poem and stacks back. **Met**
   (`13 words in 10ms`). Unpredicted: **the empty Untitled vanished from the
   list on switch-away**; plausibly the app discards an untouched neutral
   song; observed-not-ruled.
8. **Re-import, then Escape.** Expected the collision dialog (met: both dates
   ISO, destructive answer set apart, Keep mine last and focused, centred).
   **Escape dismissed the dialog safely and the import stopped: no hang,
   nothing destroyed.** The path Code's pane could not test now has a real
   observation.
9. **Keep mine × 2.** Expected one dialog per song, nothing changed. **Met**,
   and it surfaced walk finding W1 (below).
10. **Keep both × 2** (Dann's own path). Expected numbered duplicates.
    **Met in substance, refuted in detail: numbering renders as `(2)`**, not
    `2`. All four songs present, originals untouched.
11. **Open a duplicate.** Expected whole and independent. **Met**
    (`13 words in 5ms`).
12. **Take the one in this file, on the open song.** Expected the one honest
    reload. **Met, with the order observed: the second dialog completed before
    the reload fired.** After the reload, walk finding W2 (below).

## Walk findings

- **W1. The collision dialog never names the song it is asking about.** Two
  identical dialogs in a row read as one stubborn dialog, and with equal dates
  the singer cannot tell which song each answer touches. The fix is a change
  to approved copy (the strings carry no song name), so the revised English
  and French lines go to Dann with the full strings table before anyone types
  them.
- **W2. After the one honest reload, the paper arrives blank.** The source
  text returns to the textarea but nothing renders until Transcribe is
  clicked. Whether this is the ruled arrival behaviour is NOT ESTABLISHED;
  candidate one-look.
- **W3. Two clocks.** The binder filename uses the Mac's local date (August
  18) while auto-names and the dialog dates use UTC (2026-08-19). Cosmetic,
  real, unassigned.
- **W4. F7 re-observation.** The one-letter auto-name did not reproduce on
  `a8a979b`; see step 2.
- **W5. Empty-Untitled discard on switch-away.** Observed, sensible,
  not-ruled; see step 7.

## Residue on the walk origin

Four songs remain in `ilya-eaxv09qx3`'s IndexedDB, two of them `(2)`
duplicates, deletable per row at Dann's leisure. Each deploy is its own
origin; nothing else was touched.

*Recorded by Fable from Dann's live observations, 2026-08-18. Every step above
was performed by Dann's own hands on the deploy.*
