# BRIEF TEMPLATE. Use this shape for every Code brief

**Created 2026-09-18, after a night that cost six passes where two would have
done. Three of those passes were spent because the desk wrote a CAUSE into a
brief from prose rather than from the file, and Code built against it.**

**THIS TEMPLATE HAS NO SLOT FOR A CAUSE. That is the point.** The desk cannot
write a diagnosis into a form that has nowhere to put one.

---

## 1. What was observed

**Only what is observable: Dann's words, a screenshot, a measured number, a test
that fails.** No explanation of why it happens. If the desk knows a `path:line`
read this session, it goes in section 2 as a fact, never here as a cause.

## 2. What is established, each line carrying its `path:line`

Facts read this session, and nothing else. **A line without a citation does not
appear.** Memory files and previous memos are leads, and they are labelled as
leads.

## 3. Measure before you change anything

What Code establishes first, and reports before writing code. **The cause is
Code's to find and state, not the desk's to supply.**

## 4. The rulings this serves

Dann's words, quoted and dated, with what has amended them since.

## 5. Constraints

What must not change, and what is out of scope.

## 6. Done when

Observable tests, each holding on EVERY case, not only the one that exposed the
problem. `WRITTEN` on the code; `DONE` is Dann's walk.

## 7. Report back

The commit, the results against section 6, and **what could not be established**.
**NOT ESTABLISHED beats a complete invented answer.**
