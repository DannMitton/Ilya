# N.67 step 6: Dann's walk on two deploys, 2026-08-18 night

**Walked by Dann in his own Chrome, curated live by Fable, one instruction at a
time, every expectation stated before its measurement. Deploys:
`ilya-16yumobac` (`b0b5e8f`) and `ilya-qudmxhw07` (`cee4572`), both carrying
step 6. This walk settles the two deploy-only items Code's memo named and
closes N.67 whole.**

## Settled

1. **Chrome does not auto-grant persistence on a Vercel origin.** The eviction
   sentence appeared on first visit to `ilya-16yumobac`, ratified copy word for
   word, in the Fit drawer; after reload it was gone and stayed gone. WALKED,
   real state, no stub. The design-era question is answered.
2. **A real score's bytes survive the corrupt-record salvage, end to end.**
   The full circle, every step observed: a song built from the Pushkin couplet
   plus the Mussorgsky `score.mxl` (E.16 harness); its record corrupted by hand
   in IndexedDB (`poem: 42`, console, `CORRUPTED 73be09c8…`); after reload the
   row kept its own stored name with the unreadable sentence beneath, Delete
   withheld, **and the score still drew** (sources re-ingest independently of
   record validity; Fable's stated expectation refuted, favourably);
   **Export this song produced an 11.3 KB binder** against 1,454 bytes for two
   scoreless songs, so the bytes were aboard; imported on a second step-6
   origin it arrived still damaged, name kept, sentence kept, **damage
   preserved rather than laundered**; repaired by console (`REPAIRED`, same
   id), the sentence cleared, the poem returned, and **FIT drew the Mussorgsky
   stave from the transported bytes, three pages, underlay intact.**

## Refutations and findings

- **The wrong-instrument detour, Fable's fault and named as such:** the first
  import target was `ilya-eaxv09qx3`, a pre-step-6 build. Old code met the
  damaged binder and nothing visibly arrived. The step was retaken on
  `ilya-qudmxhw07`. Lesson, standing: **check the commit under a deploy before
  walking new behaviour on it.**
- **W6, new.** The neutral-song discard (W5) can strand `ilya:activeSongId`:
  after the empty Untitled was discarded on switch-away, `storage.partialLoss`
  fired on a later boot, a **false alarm** whose wording tells the singer
  their songs may be gone when the app itself discarded a song it considers
  worthless. The notice behaved exactly as ruled; the trigger is the defect.
  Candidate one-look, unnumbered until Dann rules.
- **W2, re-observed twice** (the paper waits for Transcribe after arrival: on
  the honest reload and on a repaired record). Consistent behaviour, still a
  candidate one-look, unchanged.
- **W5, re-observed** (untouched neutral song discarded on switch-away).
- Old-code behaviour on receiving a damaged binder (nothing visible) was NOT
  chased; it is dead code walking and the observation is recorded only so
  nobody mistakes it for a step-6 defect.

## Residue

Walk data remains on three deploy origins (`16yumobac`: the damaged-then-
exported song; `qudmxhw07`: the repaired song with score; `eaxv09qx3`: the
step-5 four plus whatever the old importer did). Each origin is separate;
deletable per row at Dann's leisure; nothing he uses daily was touched.

**With this walk, N.67 steps 5 and 6 are both DONE by Dann's own eyes on real
deploys, and N.67, THE SAVE FUNCTION, IS CLOSED WHOLE.**

*Recorded by Fable from Dann's live observations, 2026-08-18 night.*
