# N.126 design pack: everything Design needs, in the repository

Assembled 2026-09-11 by the coordinating desk, on the N.108 pack's precedent
(`docs/sessions/n108-design-pack/`). Design reads the connected repository
(`DannMitton/Ilya`, branch `Shane`) and nothing else, so everything is here.

N.126 is **Insights**, Ilya's third document, joining Transcription and Score
markup in the desk selector's boxed set (`DeskHead.svelte:43`).

## In this folder

| file | what |
|---|---|
| `brief-to-design-n126-insights_r3_2026-09-11.md` | **the brief to answer** |
| `ruling-e44-studio-architecture_2026-08-13.md` | the Studio ruling, to be read before any GUI opinion: Studio, one takeover, anchors compose with stations |
| `s0-slate-excerpt.md` | the S0 slate answers as recorded (`docs/sessions/LOG.md:2142-2145`); an excerpt of the record, not a ruling document |
| `typography-extract.md` | ENVIRONMENT.md's `TYPOGRAPHY IN THIS TREE`, verbatim (`docs/memory/ENVIRONMENT.md:1950-1990`) |
| `app.css` | the tokens, including `--dusty-rose: #A67B7B` (`:38`) and the derived tones |
| `DeskHead.svelte` | the selector gaining its third member |
| `VoiceProfilePane.svelte` | the squircle being inherited (`:1532-1533`, `.watch-band`) and the subtitle pattern (`:552`) |
| `Loupe.svelte` | the measure-tag grammar (`:981`) and the squircled events (`:652`) |
| `TitleHeader.svelte` | the paper family Insights must resemble |
| `Paper.svelte` | the sheet itself |
| `PageFooter.svelte` | the colophon and the page count, as printed |
| `TitlePage.svelte` | the title page |
| `sibling-transcription.png` | one rendered Transcription page, as the singer sees it |
| `sibling-score-markup.png` | one rendered Score markup page, as the singer sees it |

The two renders are not source screenshots. They are the running app at
desktop width (1440 CSS px, 2x), booted on the Sunless fixture already in the
tree (`apps/web/src/lib/shane/ingestion/fixtures/sunless-01-engraved.musicxml`),
with the fixture's own lyric in the field. Transcription comes out one page;
Score markup comes out three, and its third page carries the empty-state panel
that stands where nothing has been measured, which is the face of the brief's
curation criterion 6.

## Elsewhere in the repository, already there

| path | what |
|---|---|
| `docs/memory/PRODUCT.md` | what is settled about Ilya, including Fit: "Forecasts, never declares" (`:41`) |
| `docs/memory/CONTRACT.md` §5 | house style (`:436`) |
| `apps/web/src/lib/i18n.ts` | the strings table, including `profile.subtitleNamed` and its guillemets (`:1111`) |

## Two ruled lines, verbatim

> Inks derive through the luminance-keyed system. Ruled by Dann in the S0
> slate answers, 2026-08-19.

> Insights' governing colour is dusty rose, the existing token
> `--dusty-rose: #A67B7B` (`app.css:38`). Ruled by Dann 2026-09-11.

## What Design owns

Composition, hierarchy, and type per the ruled system; the squircle's carriage
of the anatomy; both orientations; the selector's third segment. Design does
not choose content, does not name anything, and does not write French.
