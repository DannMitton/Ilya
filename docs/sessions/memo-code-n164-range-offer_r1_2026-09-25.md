# Memo from Code, r1: N.164 range offer, built on `Shane` at `933bbda`, tree dirty with this work only (plus the desk's `draft-n172` and `brief-code-n168` r2, untouched)

**Diff.** `i18n.ts`: four `insights.offer.*` keys, EN and FR as ratified; `cannotSay` kept, unrendered, commented. `insights.ts`: `verdictLine` and `printsNoFindingsLine`. New `range-offer-decline.ts`: one key, `ilya:rangeOfferDeclined`, never expires, every call guarded. `InsightsPane.svelte`: the offer, the decline, and the findings section hidden when empty; print hides "No thanks" and plains the link. `CalibrationWizard.svelte`: `openRequest` prop, answered once per `n` (the wizard stays mounted while stowed); a capture in flight is never interrupted. `+page.svelte`: `openRangeFields` raises the drawer on a phone.
**DESK DEFAULT (Code's).** The offer needs `cannot-say` **and** no typed range. A typed range with no pitched note is also `cannot-say`, and "doesn't know your range" would be false there, so that case prints nothing.
**Tests.** 9 new (5 in `insights.test.ts`, 4 in `range-offer-decline.test.ts`). Gates: 216, 235, 0 errors and 12 warnings in 5 files, **1421** (1412 + 9; the ship script's gate 4 needs `1421 passed (1421)`), 603 with 5 skipped.
**Walked** on the local dev server, not the alias, with a seeded voice (five readings, no range) and two fixtures. Every expectation was stated before it was measured, and all held: the EN and FR lines read verbatim; the link opened the takeover on Range with the drawer raised; typing A2 to F4 then **Back** printed the `range-only` verdict; « Non merci » hid the line, kept the m. 16 finding, and held after a reload and on a second song (Close Fixture), where the empty findings heading also went. Control: removing the key brought the offer back on that song. Pane storage restored afterwards; the two songs created stay in its IndexedDB.
**Report, not fixed: Done for a voice with no readings.** From the code only: Done sets `phase = 'summary'`, which shows "0 of N" and the roster, a summary that voice never saw. It cannot arise from this link: Insights builds no model without a reading, so that voice sees the withheld statement and no offer.

## NOT ESTABLISHED

The alias walk, which waits on a ship. Print on paper: the two print rules are confirmed live in the page's stylesheet, but nothing was printed. A vitest mount of the component: vitest compiles no `.svelte` file, so "survives a remount" is tested as a second read of the same store. The compass (A3 to F♯6) and the tall empty region are untouched.
