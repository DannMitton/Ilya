# Brief: N.104 takes Dann's width, then ships

**For a fresh Claude Code session, pointed at `~/Desktop/ilya-rewrite`, branch
`Shane`.** Written by the desk 2026-08-29 on Dann's rulings of the same day.

---

## 1. Where this starts

N.104 is BUILT and UNSHIPPED. HEAD is `e19c1b9` (2026-08-27 23:15 local). The
working tree carries four modified files under `packages/score-parser/src/`
and two untracked documents under `docs/sessions/`. Nothing is committed.

Read first, both in the repository:

- `docs/sessions/memo-n104-tacet_r1_2026-08-27.md`. The build's own memo. Read
  it in full. It is the authority on what exists, what was measured, and what
  is NOT ESTABLISHED.
- `docs/sessions/brief-n104-tacet-measures_r1_2026-08-27.md`. The brief that
  produced it, for context only.

`docs/memory/STATE.md` does not yet record that N.104 is built. That is a
close-of-session repair on the desk, not yours.

---

## 2. What Dann ruled, 2026-08-29

He opened `docs/sessions/drawing-n104-tacet-weights_r1_2026-08-27.html` and
chose from its four sections.

| section | number | ruling |
|---|---|---|
| A, how long the tacet bar is | `measureSp` | **8**, down from the built 12 |
| B, how big the numeral is | `numeralScale` | 1, as built |
| C, how far the numeral clears the staff | `numeralClearanceSp` | 0.9, as built |
| D, what the count does to the mark | the mark itself | as built, approved |

`barInsetSp` at 1.25 was not among the drawing's sections and stands as built.

**On memo §7, the correction surface over a tacet run: no change this ship.**
This is DESK INFERENCE, not Dann's ruling, and it is free for him to wave off.
The reasoning: §7's option 2 bounds the two neighbouring hit rectangles at the
run's barlines, which shrinks touch targets the run had stretched to 87 and
94 px, on a surface whose portrait hit rectangle already measures 28.3 px
against a 44 px floor. Option 1 needs a readout string in both languages,
which is its own gate and its own French table. The built behaviour therefore
stands: a tap on the mark resolves to the nearer neighbouring note, and the
stepper crosses the run in one press.

---

## 3. The change. One literal and its comment

`packages/score-parser/src/staff-renderer.ts:326`

```
  measureSp: 12,
```

becomes

```
  measureSp: 8,
```

Rewrite the comment above it so it records whose eye chose 8 and against what.
It currently says 12 was "chosen at the browser against the engraved Without
Sun song 1". It becomes **Dann's eye, 2026-08-29, from the three weights in
`drawing-n104-tacet-weights_r1_2026-08-27.html`**, recorded in the same
register the tie's 0.40 is recorded in. Keep the two sentences that say the
width is fixed for every count and that the number is convention rather than
Gould.

**Change nothing else.** `barInsetSp`, `numeralClearanceSp`, `numeralScale`,
`DIGIT_SMUFL`, `tacetRuns`, `layoutColumns`, the drawing pass,
`page-layout.ts`, and all eleven tests stand exactly as they are.

---

## 4. What to re-measure, and why

The memo's §5 table was measured at `measureSp` 12. Eight is a narrower bar,
so the packing changes and the table goes stale. **State your expectation and
your likeliest failure mode in the message before each measurement**, then
report against both.

1. **The engraved song.**
   `~/Downloads/Mussorgsky - Sunless 01 - Within Four Walls (engraved).musicxml`.
   Report at 8: the system split, the score page count, `PAGE 1 OF n`, the
   first system's first note `x`, and which source measure draws first. At 12
   these were 7 systems (`0-2`, `3-5`, `6-8`, `9-11`, `12-14`, `15-16`,
   `17-17`), 1 score page, `1 of 2`, x = 157.44, and source m. 1 as a whole
   rest.
2. **The lone last measure.** At 12 the last system was `17-17`, 245.03 px of
   a 624 px line, 39.3 percent, which memo §5 named as the first document in
   this project that visibly wants C11's rebalance. Report what it is at 8.
   **Do not build C11.** It is a separate ruling and a separate change to the
   packer.
3. **The mark's drawn width.** At 12 the one tacet measure came out 97 px
   against a sung bar's 227 px. Report both at 8.
4. **The hand-built long run.** Memo §10 built a copy of the engraved file
   with the vocal notes stripped from source measures 5 to 9 and 14. Rebuild
   it and confirm the `5` numeral is still centred on the mark and still
   inside the system's viewBox crop. **A narrower bar is exactly where a
   two-digit count could overflow the mark it sits on, so measure a 13-bar run
   as well** and say whether the digits stay inside the H-bar's span.
5. **The Lamm page read.** Expect zero tacet marks, because memo §5 measured
   that the scan carries no piano part, so every declared measure carries a
   vocal event. Sources in `~/Downloads`:
   `sunless-01-v-chetyryokh-stenakh_lamm-scan.pdf` through Ilya's PDF route,
   or `sunless-01-v-chetyryokh-stenakh_lamm-scan.png`.
6. **Print.** Memo §5 walked all 36 stylesheets and found no print rule that
   hides the tacet group or any of its 14 ancestors. A width change cannot
   move that, so re-state it rather than re-walking it, and say that is what
   you did.

---

## 5. Gates and the ship

Five gates at baseline. Gate 5 is expected at **`481 passed | 5 skipped
(486)`**, which is the count memo §9 measured for the eleven new tests. This
change adds and deletes no test, so it should not move.

**`~/Downloads/ilya-ship.sh` is already updated. Do not edit it.** The desk
changed line 80 over the bridge on 2026-08-29; md5 was
`b2091010c5adf5651ad380d5f706d009` before and is
`f90fae3ebe70e5db6b95eacac1b7d58b` after. Line 79 stays at
`900 passed (900)`.

**If gate 5 prints anything other than `481 passed | 5 skipped (486)`, stop
and report it. Do not adjust the script to match the reading.**

Before the ship, Dann must add the two untracked documents, because the ship
script refuses on untracked files. Hand him these two blocks, in this order,
one at a time:

```
cd ~/Desktop/ilya-rewrite && git add docs/sessions/memo-n104-tacet_r1_2026-08-27.md docs/sessions/drawing-n104-tacet-weights_r1_2026-08-27.html docs/sessions/brief-n104-ship_r1_2026-08-29.md docs/sessions/gould-bar-numbers-p484_2026-08-29.md
```

```
sh ~/Downloads/ilya-ship.sh "N.104: the page shows every bar the singer counts"
```

**You do not run git and you do not commit.** Dann runs both blocks.

---

## 6. Definition of done for this session

Done is: the literal changed, its comment rewritten, §4's six measurements
taken and reported, five gates at baseline, the two blocks handed to Dann, the
ship landed, and the memo written. **The walk is Dann's on a deploy and is not
yours to claim.** Report the build as WRITTEN. He turns it to DONE.

---

## 7. The return memo

`docs/sessions/memo-n104-ship_r1_2026-08-29.md`, with these sections:

1. What changed. The one literal and the comment, quoted.
2. §4's measurements, each with the expectation you stated before it.
3. The lone-last-measure finding at 8, and whether C11 is still visibly wanted.
4. Gates: expected against got, in a table.
5. What you looked at with your own eyes, at what viewport.
6. **NOT ESTABLISHED**, as its own section.

---

## 8. Constraints

- **NOT ESTABLISHED beats a complete invented answer.**
- Every claim carries a `path:line`, a run, or "not established". There is no
  fourth form.
- Do not read or change anything under
  `apps/web/src/lib/shane/reconciliation/`, and do not change
  `VocalLineEvent`.
- Coin no French and add no user-facing string. Memo §8 established that
  nothing inside the notation SVG carries an accessible name today, and this
  change does not alter that.
- Do not trust a number your own script printed. Verify the rendered result,
  not the source.
- House style, which is packaged as a skill: Google's developer documentation
  style guide, Canadian spelling, and no em dashes ever.

---

## 9. After the ship, one drawing. Nothing built from it

**Added 2026-08-29 on Dann's ruling that the page must carry bar numbers,
rewritten the same evening when he photographed Gould p. 484.** Do this only
after N.104 has shipped and its memo is written. **Build nothing here. The
drawing is the deliverable.**

### Read this first

`docs/sessions/gould-bar-numbers-p484_2026-08-29.md`. It is a full reading of
a photograph Dann took of Gould p. 484, the Bar numbers section, which every
earlier extraction in this project records as outside its photographed range.
It carries the rules verbatim as `p484-a` to `p484-f`. **Do not re-derive any
of it and do not cite Gould on bar numbers from anywhere else.**

### Established by the desk this session, so do not re-derive it

Ilya draws no bar number anywhere on the page. Every text-emitting site in
`staff-renderer.ts` was read (glyphs, the octave `8`, key accidentals, the
tuplet count, rests, the tacet count, note and turning accidentals, syllables,
IPA, the phonation break) and none of them is a bar number. The data exists
unused at `Measure.number` (`types.ts:215-225`), the source's own display
string, kept as a string so pickups can be `0`, empty, or `X`.

The page number is roman: upright, `var(--font-sans)`, 9.5pt, all-small-caps,
`PageFooter.svelte:235-241`, in the footer.

### What is settled, and by whom

| | ruling | source |
|---|---|---|
| which bars | the first measure of every system | Dann 2026-08-29, and Gould `p484-c` in her own words |
| and also | the measure after a multibar rest when it lands mid-system | Dann 2026-08-29. Gould does not say it; the extraction's DESK READING under `p484-c` argues it survives Gould |
| never | every five or ten bars | Gould `p484-c` |
| **not labelled** | **the first bar of the piece** | Gould `p484-a`, **RULED IN by Dann 2026-08-29.** It amends the rule as he first gave it |
| where | above the staff, and Gould sharpens it to **above the clef** at the system's left edge | Dann 2026-08-29, Gould `p484-c` |
| slope | **italic** | Gould `p484-d`, and Dann's own instinct before he read it |
| framing | no box, no circle | Gould `p484-e` is conditional on rehearsal figures, which Ilya has none of, so this is convention rather than a rule |

### What is NOT settled, which is what the drawing is for

Gould gives no size, no weight, no clearance, and no horizontal offset. Those
stay convention, recorded as convention the way `TACET_REST` is. The
post-rest number has no clef beneath it, so its horizontal anchor is an open
question that the system-start number does not have.

### The drawing

Build `docs/sessions/drawing-bar-numbers_r1_2026-08-29.html`, in the shape of
`drawing-n104-tacet-weights_r1_2026-08-27.html`: every plate the real
renderer's real output, each section at true size and again at three and a
half times, self-contained with the font embedded and no external references.

| section | what varies | plates |
|---|---|---|
| A | size and weight of the italic number, above the clef | three sizes, stated in stave-spaces. Add ONE roman plate at the middle size for contrast, labelled as the option Gould `p484-d` argues against |
| B | clearance above the top staff line, and the horizontal anchor of the post-rest number | three clearances; anchors over the barline, over the measure's first note, and split between them |
| C | the post-rest number, which **Dann calls "a useful courtesy"**, his word, 2026-08-29 | bare, against parenthesized. The courtesy framing argues for the parenthesis: it is the courtesy accidental's own convention, and r178 p.490 parenthesizes a continuation system's number for a related reason. Not ruled |
| D | **the crowded band** | one real system carrying a bar number, a tuplet count, and a tacet count at once |

**Section D is the one that matters and prose cannot settle it.** After N.104
the band above the staff holds three kinds of number.
`staff-renderer.ts:1118` already sets the tuplet count italic at 11 px, pinned
by a test at `:290`, and it is the only italic mark in the notation, so an
italic bar number shares a face with it. Gould separates them by position
rather than by face. D exists so Dann can see whether that holds on a real
page.

Report in the same memo, under its own heading: what you drew, what each plate
costs in vertical room, and any collision you found. **Propose nothing and
build nothing.** Dann chooses, then a second brief builds it.
