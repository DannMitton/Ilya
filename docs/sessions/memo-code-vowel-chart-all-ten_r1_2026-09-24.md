# MEMO. Five walk findings: WRITTEN, not shipped

`Shane` at `bb8071c`, tree dirty. Gates at baseline, except eight new tests: 1401 and 602.

1. **Vowel chart.** Ten rows in `VOWELS` order. Sunless 2 prints [a] "0 s", no bar, both languages. Sunless 1's page one clears the foot by 126.2 px (English) and 78.6 px (French).
2. **Score words on Text.** Derived, never written at startup. A song with stored placements is never claimed, and N.160's heal is untouched: there a stress change writes nothing and lasts one session. Without Sun no. 1 (scratch copy, poem emptied): 96 of 96 identical after the act and after a reload. Cupid: the act writes the poem and seats 146, which a reload keeps.
3. **"1 line"** fixed.
4. **Spaced flat.** `app.css:23`: no serif face draws ♭ tight, so it takes 12.5 px, English too. Table accidentals now use the sans: 5.33 px.
5. **Pickup meter.** The `.musx` stores bar 1 as 2/4 displayed as 4/4; denigma drops the display. `pickup.ts` fixes both parsers. Sunless 2 prints 4/4 once; phonation unchanged.

## NOT ESTABLISHED

- Dann's live Without Sun no. 1.
- A gloss change on such a song: not walked.
- Whether a visible tab overflows page one on first render (hidden pane: 74.8 px).

**`git add`:** `packages/score-parser/src/pickup.ts`, `pickup.test.ts`, this memo.
