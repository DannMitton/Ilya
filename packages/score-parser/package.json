{
  "name": "@ilya/score-parser",
  "version": "0.1.0",
  "private": true,
  "description": "Shane score parser: dual-canonical (MNX + MusicXML) ingestion into ParsedScore, plus Verovio renderer output",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
