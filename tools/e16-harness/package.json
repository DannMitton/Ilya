{
	"name": "e16-harness",
	"private": true,
	"version": "0.1.0",
	"description": "E.16 evaluation harness: render a corpus .musx to a full-page image, extract ground truth from the same .musx, and score a recognizer's output against it in Fit units. Evaluation tooling, NOT product code; not imported by apps/web or packages/*.",
	"type": "module",
	"scripts": {
		"self-test": "node src/self-test.ts",
		"run": "node src/run-harness.ts"
	},
	"dependencies": {
		"verovio": "^6.2.0"
	},
	"engines": {
		"node": ">=22.6.0"
	}
}
